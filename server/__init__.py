# -*- coding: utf-8 -*-
"""ECHO 能力后端（服务端）。

**这个包只认识"能力槽"，不认识业务概念。**

它做三件事：把一段音频变成文本 / 时间轴 / 嵌入；声明自己能做什么；
把客户端的每次调用记成**元数据**。它不存会议、不存文本、不做身份判断。

四条禁令由 `tests/test_server_contract.py` 钉住（不是靠自觉）：
  * 源码里不出现业务词；
  * 不 import 客户端的业务层（db / settings / 业务编排）；
  * `/v1/*` 没有任何写端点；
  * 跑完一次请求不留内容。

为什么单独一个包而不是塞进 `app/`：物理隔离比纪律可靠 —— 放进 `app/` 就会忍不住 import。
设计文档：`docs/ECHO能力后端-服务端设计.md`。
"""
__all__ = ["__version__"]

#: 服务端自己的版本（与客户端 `app.__version__` 无关，两者独立发版）
__version__ = "0.1.0"
