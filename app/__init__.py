"""ECHO — 个人语音助理核心包。

版本号是**唯一权威来源**：`app/__init__.py:__version__`。
其它地方（`app/api.py` 的 /api/status、`app/main.py` 的 FastAPI title、
`pyproject.toml`）都必须与它一致，由 `tests/test_version.py` 兜住。
发布流程见 docs/REFACTOR-PLAN.md §13。
"""
__version__ = "1.0.0"

