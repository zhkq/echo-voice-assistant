# ECHO 3.0 进度（后端已落地）

> **这份文件的用途**：让任何人（或下一轮对话）**冷启动就能接手**。
> 只记"现在到哪了、决定是什么、坑在哪"，设计理由都在那四份设计文档里。
> 记录时间：2026-09-24。

- 分支：**`feat/3.0-backend`**（`87d44f20`），已推送并与 `origin` 同点
- `main` 停在 `3b217355`（2.0 最后的修复）—— **3.0 还没回合进 main**
- 稳定版 `D:\ECHO` 停在 2.0（`ECHO-kit-20260924-0126.zip`，git=3b217355）；
  **本次没同步稳定版**（按 AGENTS.md：只在明确指令时做）
- 验收门槛：`scripts/check-windows.ps1` 全绿 —— 最近一次 **1142 项**（skip 9）

---

## 一、一句话现状

**客户端的能力层（`app/capabilities/`）已经落地** —— 能按槽在本机与 ECHO 后端之间选、
失败换下一个、并强制三条铁律。**但还没有任何主链路在用它**：
`meeting.py` / `assistant.py` 仍然直接调 `app/audio/*`，
面板也还没有「能力路由」页签。

后端（`server/`）已经能接客、能鉴权、能自测。

## 二、已完成

### 2.1 服务端 v1（`server/`，16 个文件约 2400 行）

| 文件 | 干什么 |
|---|---|
| `main.py` | app 工厂 + 生命周期 + **命令行管理入口**（发码 / 列客户端 / 撤销） |
| `routes.py` | `/v1/*`：能力端点 + 配对/令牌 + **两级闸门 + 廉价预检** |
| `pool.py` | `EnginePool`：单飞加载、引用计数、LRU、显存预算（**绝不 OOM、绝不回退 CPU**） |
| `engines.py` | 把 `app/audio/*` 包成池能持有的加载器（**唯一的借用面**） |
| `auth.py` | 配对 / PBKDF2 secret 哈希 / 短期 JWT / scopes / 撤销 + 跨进程轮询 / 限速 |
| `store.py` | **唯一落库处**：`clients` + `pairing_codes`（设计 §8.5 白名单的子集） |
| `tmp.py` | 临时文件：按日期落盘 + finally 删 + 每小时扫 + 总量上限 |
| `audio.py` | 收音频（raw body、边收边算）+ 规格化 16k + **`drain()`** |
| `settings.py` / `errors.py` | 配置（YAML+env）/ 机器可读错误码 |

- 端点：`GET /v1/capabilities` `/v1/health` `/v1/ready`；
  `POST /v1/pair` `/v1/token`；`POST /v1/asr` `/v1/diarize` `/v1/speaker/embed`
  （路由表**恰好**等于白名单，多一条用例就红）
- 部署：`Dockerfile` + `compose.yaml`（只读根 + **两个保留策略相反的卷**）+ 示例配置
- 自测：`scripts/smoke-echo-backend.py`（21 项）+ `server/README.md`

### 2.2 拍板记录（别再重新讨论）

| # | 决定 | 在哪 |
|---|---|---|
| 1 | `variant` 取值 `short\|long`（**不是** `meeting` —— 源码不许出现业务词） | 服务端 §6.5 |
| 2 | **出厂清单不放常驻小模型**（去掉 `asr-short`）；短请求由 `asr-long` 的 `supports: [asr.text]` 兜住 | 服务端 §14-2 |
| 3 | 闸门**在收音频之前**做一次廉价预检（满了不读 body 直接拒）；权威判定仍是 `hold` | 服务端 §14-4 |
| 4 | 鉴权**提前到 v1.5**（安全边界不该等配额一起做） | 服务端 §13 |
| 5 | `token`（静态，单人）与 `jwt`（配对，多人）两种模式并存 | 服务端 §7 |

### 2.3 实测挖出来的问题（都已修 + 已钉）

| 问题 | 症状 | 钉在哪 |
|---|---|---|
| 幽灵槽 | `variant=short` 一律 404（要的 `asr.short` 没人提供），默认 `long` 把用例全带绿 | `SlotRoutingTests` |
| 宣告 ≠ 可路由 | `capabilities` 说能做 `asr.timestamps`，路由却 404（同一份事实算两遍） | `SlotRoutingTests` + `pool.slots()` 单一来源 |
| 静默回退 CPU 只堵住一半 | 判据笼统问 CUDA；**向量类加载器压根没查设备**，pyannote 那句 CPU 回退在服务端可达 | `DeviceAssertionTests` |
| **撤销在唯一运维入口上是假话** | 命令行 `--revoke` 后，跑着的服务继续接受旧 JWT（轮询没实现） | `AuthTokenTests`（跨进程用例） |
| **持久凭据落在可丢弃目录里** | 鉴权库缺省落 `tmp.root`，而文档建议 tmp 挂 tmpfs → 所有客户端一起没了 | `EnvOverrideTests` |
| 部署文件里的环境变量没被读 | `ECHO_STATE_ROOT` 只写在注释里 | `EnvOverrideTests` |
| **`max_concurrent: 2` 是假的** | `async def` 端点里阻塞推理按住事件循环 → 请求全串行，`busy.active` 全程 0 | `EventLoopNotBlockedTests` |
| **大 body 出错时错误码消失** | 没读完 body 就关连接 → RST 丢掉响应体；2 KB 正常、0.9 MB 为空，且是竞态 | `ErrorCodeSurvivesLargeBodyTests`（真 socket） |
| **"唯一免凭据的端点"是错的** | 设计 §6.1 说 `/v1/pair` 是唯一免凭据端点，实际 `health`/`ready`/`capabilities` 三个 GET 也敞开（处理函数没调 `client_of`）。查下来**敞开是对的、文档写窄了**，已把公开表写准 | `PublicSurfaceTests`（遍历 OpenAPI，新端点忘接鉴权就红） |
| 抄错的令牌被说成"网络坏了" | 令牌里混进中文 → urllib 抛 `'latin-1' codec can't encode…`，兜底报成"连不上 <地址>"，**指错方向** | `test_a_token_with_chinese_in_it_says_so_instead_of_blaming_the_network` |
| **秘密保护被写在了业务代码里**（违反 D12） | `credentials.py` 直接写了 DPAPI 与 `os.name` 分支 → 闸报 `PLATFORM_TOKEN`。已搬进 `app/platform/`（三个平台同名原语），业务代码从此没有平台分支 | `test_path_seam.py` 的 `PLATFORM_TOKEN` + 原语清单 |
| 护栏自己误报：`"%s://%s"` 被当成 `s:` 盘符 | `pairing.py` 拼后端地址的格式化模板被判成盘符路径（`http://` 当初躲过是因为 `p` 前面还有 `t`） | 收紧 `audit-paths.py` 的正则 + 自证用例 `test_ignores_a_format_placeholder_before_a_scheme` |
| 确认名单多留了一项 | `capabilityPrivacy` 现在有直接字面量读法，闸报"已不再是间接读，请从名单里删掉" —— **防腐机制正常工作** | `test_settings_wiring.py::test_acknowledgement_lists_do_not_rot` |
| **新代码里的 F821 只有闸能抓** | `capability_admin.py` 用了 `Optional` 却没 import：模块开了 `from __future__ import annotations`，注解不求值 → **用例全绿**，只有 ruff 的 F821 报。教训：每切一小片就单独跑一次 `ruff --select F821,E9`，别攒到整闸 | `scripts/check-windows.ps1` 的 ruff 步 |
| **我自己的新用例去动了真实状态目录** | `QuotaAdminTests` 第一版没给 `--config`，命令行入口于是开了**真实的** `data/server-state` 库（报"没有这个客户端"，因为它在看另一个库）。照 `AdminCliTests` 写临时配置修好，并删掉跑出来的那个库 | `QuotaAdminTests` 的注释 |
| **观察到一次未复现的偶发红灯**（未定位） | TLS 那一片的闸第一次跑 `FAILED (errors=1, skipped=11)`，而**同一棵树**随后两次单跑与一次整闸都是 `OK`。当时闸的输出被过滤掉了，没留住 traceback —— 所以**没有答案，只有事实**。下次再遇到：闸的输出**必须**留档（`Tee-Object`），否则一条偶发错误追不回来 | —（未定位。记在这里是提醒：**"闸绿"必须能复现才算数**） |
| **同一族第二次：闸"红"但用例全绿**（2026-09-24 深夜，有证据了） | 落布局那一轮跑闸：`Ran 1519 tests` → **`OK (skipped=11)`**，接着进程以 `exit=-1073740940`（`0xC0000374` = **STATUS_HEAP_CORRUPTION**）退出，闸据此判 FAIL。崩溃前最后两行是 `[recorder] 录音失败: Stream is stopped [PaErrorCode -9983]` + `[assistant] 已有命令流进行中` —— 指向 **PortAudio/sounddevice 在解释器退出时的析构**（原生扩展里堆被写坏）。**用例结果是绿的，红的是退出码**；本轮先如实记下、没排（要排得先能稳定复现，估计得单独跑 `tests/test_audio*` 那一片）。判据留给下次：`OK` 出现在 `Ran` 之后 = 测试通过，退出码红是另一件事 | 证据在 `%TEMP%\echo-gate13.log` |

**共同形状**：都是"用例全绿、东西是坏的"，而且**只有真的把服务跑起来发请求才会暴露**。
所以两条通用教训值得记住：

1. **测试手段必须配得上被观测的现象** —— `TestClient` 会把请求串起来发，
   测不出并发；进程内 ASGI 不经过 socket，测不出 RST。
2. **同一份事实只许算一遍** —— 槽表、`vectorSpaceId`、配置默认值，
   凡是两处各算一遍的，迟早漂。

## 三、明确**没做**的（别在文档里读成已完成）

| 项 | 属于 |
|---|---|
| **内网公共 ASR 适配器**（施工顺序 step 2）+ 探测 + 熔断 | step 2。**卡在三件事上**：那台服务是 **WebSocket**（不是 OpenAI 兼容，`能力路由` §8.2 已更正）、协议/时间戳/配额要先问同事（§8.2.2）、以及"要不要为它给客户端引一个 WS 库"这个岔路（§8.2.1） |
| **会议链路接 `diarize.*` / `speaker.embed`**（"会议侧接入"） | step 4。**2026-09-24 真机联调时确认**：面板那两行设置是真的、路由表也认识这些槽，但 `app/meeting.py` 的分离那一步是**直接调本机** `app/audio/diarize.py`（`meeting.py:733`），全仓**没有任何消费方**向路由要过 `diarize.*` / `speaker.embed`。后果很具体：办公本（默认档没装 pyannote）现在能靠后端转写，**但拿不到说话人** —— 而 L2（"客户端不产生向量"）正是为此立的。要一起做的是 §6.3 的**会议级向量空间锁定**（`SAME_SOURCE_SLOTS` 已在路由层强制，落库字段与 `meta.json` 还没写） |
| `calls` 审计表 + 进程内 metrics | ✅ **已落地**（§2.10） |
| `calls_rollup`（小时/天聚合）+ SSE 进度 | v2 的剩余部分。**在管理面需要跨月视图之前不做**（`calls_summary()` 直接在这一张表上聚合就够） |
| 管理面（§8.4 五个页签） | ✅ **只读版已落地**（§2.14）：独立端口 + 登录 + 五个页签。**写动作刻意仍在命令行**。2026-09-24 真机跑通（§2.16） |
| 管理面的**写动作**（在网页上撤销/禁用/换 secret/改配额） | **刻意不做**。要加先回答"它比在命令行敲一条命令多解决了什么？" |
| secret 轮换的 24 小时宽限期 | ✅ **已落地**（`--grace-hours`，见 §2.11；含一处设计更正：服务端只存哈希，发不出新 secret，所以"自动换新"改成了"通知 + 发新码"） |
| TLS（`server.tls`） | ✅ **已落地**（服务端配置 + 客户端证书固定，见 §2.12）+ **配对串里的 `fp=` 现在真的有了**（§2.16）。**部署时仍建议反代或独立网段**——TLS 保护的是传输，不替代网络隔离 |
| `est_vram_mb` / `max_concurrency` 的真机校准 | **仍是占位值**，要真 GPU 机器压测 |
| 桌面客户端与 DSH 的目录分离落地（L6） | 安装侧 |
| **（不再列在这里）** 会议详情展示执行计划 ✅、配额 ✅、`POST /v1/tts` ❌ **拍板不做**、`mode=turns` ❌ **拍板不做** | 见 §2.7 / §2.8 / §2.9 |

### 3.1 待拍板：降级原因词汇表不够用（2026-09-24 落地客户端时发现）

权威词汇（`统一路由` §2）只有十个：`absent / blocked / busy / circuit-open / offline /
open-failed / quota / unsupported / vector-mismatch / error`。但服务端 §6.3 的错误模型里
有几档它**明确要求分开**的：

| 服务端坚持要分开的 | 权威词汇里有对应词吗 |
|---|---|
| `client_busy`（自己占着，重试永远没用） vs `server_busy`（系统忙，该退避重试） | ❌ 只有一个 `busy` |
| `model_loading`（该等一会儿） | ❌ 没有"正在加载"这一档 |
| `unauthorized` / `forbidden`（凭据/权限） | ❌ 没有 `auth`（`能力路由` §5.2 那版**有**，权威版没有） |

**服务端那句话是**："合并成一个码，客户端就只能盲目重试" —— 而权威词汇把它们合并了。

**当前的做法（信息一点没丢，也没擅自扩词汇）**：两套词各管一件事 ——
* `reason`（权威词汇）只回答"**这一轮换不换这个后端**"；
* `code` + `retryable`（本层自己那张表）回答"**客户端该干什么**"
  （`client_busy` → 不重试；`server_busy` → 退避重试）。

**建议拍板**：要么给权威词汇加 `loading` / `server-busy`（`auth` 那档也补回来），
要么在 `统一路由` §2 里写死一句"**词汇表只管候选筛选；行为一律看 `code`**"。
两条都行，但**得选一条** —— 现在这个含糊是四域统一词汇这件事本身漏出来的。
契约由 `tests/test_capabilities_contract.py::ServerCodeCoverageTests` 钉着
（服务端每个错误码都必须有 reason + retry 策略，加了码不跟就红）。

### 2.5 客户端能力层（2026-09-24，施工顺序 step 0）

`app/capabilities/` 七个文件：

| 文件 | 干什么 |
|---|---|
| `base.py` | 契约：**槽清单**（9 个，与服务端取交集）、**降级原因**（权威十词）、结果类型（带出处）、`CapabilityClient` |
| `local.py` | 本机后端：**原样包 `app/audio/stt.py` 与 `diarize.py`，一行都没改它们** |
| `echo_server.py` | HTTP 适配器：`urllib`（客户端要轻，不引新依赖）、错误码→原因翻译、`capabilities` 取交集、**401 换一次令牌再试一次** |
| `router.py` | 按槽选后端 + 失败换下一个 + **L2/L5 同源强制** + **L3 指令钉本机** + `skipped` 带原因 |
| `assemble.py` | **拼装层**：把"文本 + 骨架 + 时长"装成句级时间轴（`exact/aligned/estimated/none`），两个后端共用同一份实现 |
| `credentials.py` | 凭据落盘（Windows DPAPI / POSIX 0600），见 §2.6 |
| `pairing.py` | 配对与换令牌（`/v1/pair`、`/v1/token`），见 §2.6 |

设置项（`app/config.py`，**全部 `hidden`**，由将来的「能力路由」页签承载）：
`capabilityEchoServerUrl` / `capabilityEchoServerToken`(secret) /
`capabilityEchoServerStaticToken`(secret) / `capabilityPrivacy` /
`capabilityMeetingAsrBackend` / `capabilityDiarizeBackend` / `capabilityEmbedBackend`。

契约用例 `tests/test_capabilities_contract.py`（45 条）的写法值得记住：
**同一份断言跑两个后端**（本机用打桩引擎、ECHO 后端用**真 uvicorn + 真 HTTP**），
要加契约就加在那一个混入类里，别只加在某一边。

### 2.6 配对与凭据（2026-09-24，客户端侧的"这一步现在真能走了"）

| 文件 | 干什么 | 用例 |
|---|---|---|
| `credentials.py` | 落盘：Windows **DPAPI**（用户作用域）/ POSIX `0600`；`accessToken` **不落盘**；读坏了返回 `None` 不抛 | `tests/test_backend_credentials.py`（19 条，含"直接读文件字节"的断言） |
| `pairing.py` | `pair()` / `fetch_token()` / `ensure_token()` / `unpair()` / `state()`；错误是**一句人话** + 服务端 code | `tests/test_backend_pairing.py`（25 条，分"线路形状"与"真 uvicorn"两层） |

三条设计取舍（都写进了服务端设计 §7.5 ⑥）：

1. **地址与令牌的来源顺序**：地址用设置里填的（显式配置该赢），没填才用配对记下的；
   令牌**手填的优先**（排障时能把凭据那一路整个绕开），手填的**不自动续期**（不知道它的期限）。
   "只配对、什么都没配"是一种能用状态。
2. **401 之后换一次再试一次**：令牌没到点上就被拒（`--rotate-secret` 或时钟偏差），
   再换一个就好。**只试一次** —— 真失效的话再换也换不来，无限重试会把一次失败变成转不出来的循环。
3. **两层的错误模型不合并**：能力调用要的是路由能用的 `reason`（十词表），
   配对是一次性交互、要的是一句人能看懂的话。所以 `pairing.py` 不 import `echo_server.py`
   （反向 import 是有的，方向单一不成环）。

### 2.7 面板出口 + 客户端接口（2026-09-24，施工顺序 step 3 的界面那半）

到这一步，"配一台 GPU 后端给会议用"这件事**人能自己走完了**：发一张配对码 →
面板里填地址与码 → 开会就自动走那台后端。

| 文件 | 干什么 | 用例 |
|---|---|---|
| `app/capability_admin.py` | 把后端与配对状态整理成面板能直接渲染的形状；配对 / 解除配对 | `tests/test_capability_admin.py`（18 条） |
| `app/api.py`（新增 4 个端点） | `GET /api/capability`、`POST …/probe`、`…/pair`、`…/unpair` | 同上（真 TestClient） |
| `web/index.html` + `web/app.js` + `web/app.css` | 「能力」页签里的一张 `capRouteCard`：配对区 + 后端清单 + 三个"用哪个后端" | `tests/test_capability_panel.py`（7 条，静态接线） |

**四件当时没想清楚、被用例逼出来的事**（都值得记）：

1. **`build_default_router` 原来只看设置里的地址** —— 于是"只配对、什么都没配"的机器
   面板显示配好了、会议却还是走本机。现在两个来源都问
   （`echo_server.client_from_settings()`，顺序也定在那里）。用例：
   `test_merely_pairing_is_enough_for_the_backend_to_show_up`。
2. **`diarize.turn_embeddings` 在路由里没有对应的设置项** —— 面板那边列了它，路由漏了，
   表现会是"设置里明明选了 ECHO、某个槽还是按自动挑"。补进 `_setting_key_for`。
   是 `test_both_directions_agree`（两个方向对一遍）抓出来的。
3. **`Settings.all()` 把 `hidden` 整行剔掉**，而能力路由这批设置**全是 hidden**；
   于是面板要么自己拼设置行（= 同一份事实算两遍，迟早漂），要么给 `all()` 加一个
   `include_hidden`。选了后者：**同一形状的行由同一个函数出**，面板直接复用
   `renderSettingRow`。默认值不变，`/api/settings` 行为一个字没改。
4. **卡片的接线要能用静态断言钉**：门禁里没有浏览器，而这块界面出问题的方式是
   "安静的空白"（id 拼错、忘了调它、卡片被放到没人切过去的页签里）。
   `cap*` / `btnCap*` 那一片的 id 引用、事件绑定、载荷字段名对 `PairBackendIn`
   都钉住了；跨语言那处（JS 送的 key ↔ pydantic 的字段）最容易悄悄断 ——
   字段名写错时 pydantic 用默认值兜住，按钮点下去毫无反应。

**没做**：这块界面的**视觉验证**（无头 Edge 截图，配方见 `docs/面板布局-窄边条.md`）。
门禁不引浏览器，所以这一步得人来做 —— 布局那几条硬约束（`min-width: 0` 之类）
已经被 `test_panel_layout.py` 钉着，但"长得难不难看"它管不了。

### 2.8 配额（2026-09-24，施工顺序 step 4 的前半）

`server/quota.py` + `clients.daily_audio_minutes` 列 + `--set-quota`。
用完回 **429 `quota_exceeded`**，`Retry-After` 指到**本地明天 0 点**。

四条实现时才定下来的语义（都写进服务端设计 §7.2 那张表，别读成"精确的日额度"）：

1. **先占通道、再记账**（`State.billing`）：被 409/503 顶回去的请求不扣额度；
   通道拿到、模型要开跑了，即使推理失败也扣 —— 那段 GPU 时间真花了。
2. **只看"已经用完"，不预测**：预测量要等解码出秒数，而那时 body 已收完，
   正是要避免的"先落盘再说不行"。边界上允许最后一次略微超额。
3. **`0 = 不限`**，剩余量在响应里**不给字段**（回 -1 或大数，界面都会当成"有额度"）。
4. **`/v1/capabilities` 只给全局上限**（它免凭据，给个人用量等于泄露别人的）；
   客户端自己的剩余量在推理响应的 `quotaRemainingMinutes` 里。

### 2.9 明确**不做**的两项（2026-09-24 拍板，理由是"没有消费方"）

那轮要把剩余功能做完时，逐条查了消费方，发现这两项在**当前架构里没有位置**。
判据不是"难不难"，是"**做完谁用**"：

| 项 | 为什么不做的证据 |
|---|---|
| 服务端 `POST /v1/tts` | 客户端**已经拍过板**：`router.DEFAULT_ORDER` 里 `"tts": ()`，注释"不走能力层（客户端自己的 providers 管）"。而且合成一句话在客户端是毫秒级本地活，走服务端变成一次往返；真做还要在 GPU 机器上选一套 TTS 权重（现在一套都没有，也没测试环境验音质） |
| `mode=turns`（逐 turn 嵌入） | `diarize.turn_embeddings` 在客户端**一个消费方都没有**：全仓只有 `base.py`（槽清单）、`router.py`（顺序 + 设置映射）、`capability_admin.py`（中文标签）提到它。服务端现在是**如实拒绝**（400 + "v1 只支持 segment"），不是假成功 |

**什么时候该做**：出现"一台没有任何可用音色的客户端"（TTS）或"客户端需要逐句说话人向量"（turns）。
在那之前，做出来就是纯负债 —— 要跟着协议/鉴权/配额/文档/测试一起演进，却没有任何东西变好。

### 2.11 轮换宽限期（2026-09-24）

`--rotate-secret ID --grace-hours 24`：宽限期内**旧 secret 仍可换令牌**，
`/v1/token` 回 `X-Echo-Secret-Rotated: 1` + `X-Echo-Secret-Expires`，客户端写一条 warn 日志
提醒人重新配对；命令行顺带再发一张配对码（运维手上有东西可交）。

**设计里那句话照字面实现不了，已更正**：原文写"服务端回 `X-Echo-Secret-Rotated: 1`
+ **新 secret**，客户端自动落盘"—— 但服务端**只存 `secret_hash`、手上没有新 secret 的明文**
（这正是"secret 只出现这一次"的代价）。所以"自动换新"做不到，只能做成上表那三件事。

**另一条边界写进了命令行的输出**：**宽限期不能用于 secret 泄漏** ——
宽限期内持有旧 secret 的人照样进得来，它换不掉一个已经泄出去的秘密；
它只解决"例行轮换不想打断客户端"。泄漏请用默认轮换（旧 secret 立刻失效、
`prev_*` 一并清空，把之前开的门也关上）。

**两种模式下 `token_version` 都 +1**：宽限期给的是旧 **secret** 一条活路，不是旧**令牌** ——
泄漏时最先想断掉的就是那些已经发出去的 JWT。8 条用例里有 3 条专门钉"它不是什么"。


### 2.12 TLS（2026-09-24）

服务端：`server.tls.certfile` / `keyfile` 两个都填才起 https，**只填一个启动就报错**
（不静默降级 —— 那会让部署的人以为连的是 https，而音频在明文上传）。
客户端：配对时**取回服务端证书并固定**（`credentials.cert_pem`），之后每次调用只认那一张，
于是**不用给每台机器装自签根**（设计 §7.5 ① 说的那件事，现在真的成立了）。

三条边界写在设计里：不传 `fp=` 时是 **TOFU**（第一次连接本身就是信任建立的那一刻）；
传了就一定要对上，对不上直接拒；**https 而没有固定证书 → 拒绝连接**，
**绝不静默跳过校验**（静默跳过的坏处不是"不安全"，而是从现象上看不出来 —— 照样能用）。

用例 18 条（真 https 握手 13 + 服务端配置 5）。证书是**内嵌的测试专用自签证书**
（`tests/tls_test_cert.py`，CN=echo-tls-test、只签 localhost），所以门禁**不需要
openssl、也不需要 cryptography** —— 不为一个 TLS 用例给门禁加依赖。

写的时候被用例抓到一个真漏洞：**`fetch_token` 忘了用固定的证书** ——
自签证书下换令牌那条路会整个不通，而"图省事把校验关掉"就成了能用但中间人随便进。


### 2.15 交付档位（施工顺序 step -1，2026-09-24）

**默认档 / 离线增强档**现在是一份机器可读的表：`components/profiles.json`
（放 `components/` 里 —— 那目录**会进主包**，面板也读它）。它声明每个档用哪份依赖清单、
装哪些组件、额外项多大、禁止出现什么，以及 §1.2 那几条验收目标。

这条约定原来散在**四处**（`requirements-core.txt` 的注释、`offline-pack.json`、
`install.ps1`、设计 §1.1 的表格），改一处就会漂；更要紧的是**它原来没人守着** ——
"默认档不含 torch"这句话，谁能保证半年后还成立？

`tests/test_default_profile.py`（15 条）盯的每一件都对应一个真实的失败方式：
往清单里加 torch 系 / **从组件那条路把 torch 带回来**（把 `stt-sensevoice` 挪进默认档，
依赖清单一个字都不用改）/ 忘了装 `sherpa-onnx`（**2026-09-23 真实事故**）/
加了组件忘了改合计 / 组件孤儿。两条做过变异检验：加 `torch>=2.0` → 红；
挪 `stt-sensevoice` → **三条同时红**。

**仍然待办（要一台干净机器）**：实测"装完 < 3 分钟 / 冷启动 < 10 秒 / 常驻 < 500 MB"
与真实解包体积 —— 那三条不是这组用例能证明的，所以**没**写进"已完成"。


### 2.14 管理面（2026-09-24 起只读 → **2026-09-25 起可写**：发授权 + 客户端管理 + 审计）

`server/admin.py` + `server/admin.html`：`server.admin_listen` 一配就起一个**独立端口**
上的管理页面（概览 / 模型 / 客户端 / 调用 / 存了什么）。
账号只能从命令行建（`--new-admin` 等）—— 因为管理面**自己没有写端点**。

**为什么刻意只读**：那五个页签要回答的是"模型跑得怎么样、客户端有谁在连、谁调了什么"，
全是**看**；而写动作命令行已经齐了，且那条路直接开库、不多开一条 HTTP 入口。
`IsolationTests.test_the_admin_api_has_no_write_endpoints` 钉着这件事 ——
真加了写端点，那条会红，加的人必须在提交里说明理由。

安全上几处**写明的选择**：口令用 stdlib 的 `scrypt`（设计写的是 argon2id，
但它要引依赖；偏离写在设计里、可平滑迁移）；会话在**进程内**（重启即登出，
管理面不该水平扩）；登录防爆破；**不区分"没有这个用户"与"口令不对"**
（用例比的是**两个响应体逐字相同**）；被禁用的账号**手上会话立刻作废**。

用例 31 条。写的时候被抓到三次，三次都值得记：

1. **`admin.py` 里出现了业务词** —— 我那个"内容列黑名单"必然要逐字列出那几个概念，
   而服务端源码里**不许出现它们**（那条护栏的全部意义）。改成"服务端只报事实、
   判断留在测试里"。**更细的坑：那条护栏连注释一起扫** —— 我第二版在注释里写出那个词，
   照样红。同理 `summary` 这种普通英文词也撞线，改名 `aggregate`。
2. **`verify_password` 遇上坏哈希会抛**（`scrypt$!!$!!` → base64 解出空值 →
   `dklen=0` → `ValueError`）。那会把"登录失败"变成"服务端 500"。
3. **我漏了 `install_paths_seam` 的还原** —— `create_app()` 是**进程内全局** monkey-patch，
   不在用例里还原会连累排在后面的测试文件。有一条专门的护栏扫这个
   （`PathsSeamTests`），第一遍就是被它抓住的。



服务端：`server.tls.certfile` / `keyfile` 两个都填才起 https，**只填一个启动就报错**
（不静默降级 —— 那会让部署的人以为连的是 https，而音频在明文上传）。
客户端：配对时**取回服务端证书并固定**（`credentials.cert_pem`），之后每次调用只认那一张，
于是**不用给每台机器装自签根**（设计 §7.5 ① 说的那件事，现在真的成立了）。

三条边界写在设计里：不传 `fp=` 时是 **TOFU**（第一次连接本身就是信任建立的那一刻）；
传了就一定要对上，对不上直接拒；**https 而没有固定证书 → 拒绝连接**，
**绝不静默跳过校验**（静默跳过的坏处不是"不安全"，而是从现象上看不出来 —— 照样能用）。

用例 18 条（真 https 握手 13 + 服务端配置 5）。证书是**内嵌的测试专用自签证书**
（`tests/tls_test_cert.py`，CN=echo-tls-test、只签 localhost），所以门禁**不需要
openssl、也不需要 cryptography** —— 不为一个 TLS 用例给门禁加依赖。

写的时候被用例抓到一个真漏洞：**`fetch_token` 忘了用固定的证书** ——
自签证书下换令牌那条路会整个不通，而"图省事把校验关掉"就成了能用但中间人随便进。


### 2.13 WebSocket 传输层（2026-09-24，step 2 里"能自测的那一半"）

`app/capabilities/wsclient.py`：RFC 6455 客户端那一半，**只用 stdlib**
（握手 + accept 校验、掩码、三种长度分档、分片重组、ping→pong、关闭、超时、单帧上限）。
配一个**独立实现的桩服务端**（`tests/test_ws_client.py`，19 条，桩那边逐字节按 RFC 拼解，
**刻意不复用被测代码**）。`wss://` 复用 `pairing.pinned_context()` 的证书固定，
**没给上下文就拒绝连接**。

**它现在没有任何 import，而这件事有一条用例钉着**（`NothingImportsItYetTests`）——
等 `intranet` 适配器接上，那条会红，顺手删掉。没有它的话，"还没接"只存在于文档里。

**还缺的是协议本身**（设计 §8.2.2 的三件事：帧格式 / 时间戳 / 配额），所以适配器仍然不写。

**教训（变异检验那次）**：我第一次做变异时把 `accept_key` 换成恒等函数 —— 那是
把比较**改得更严**，不是"不校验"，于是用例照样红，我差点得出"这条护栏没牙"的假结论。
改成**源码级**替换（`if got != accept_key(key):` → `if False:`）才真的打中。
**变异方向错了，得到的是关于自己的假结论**，不是关于代码的结论。



服务端：`server.tls.certfile` / `keyfile` 两个都填才起 https，**只填一个启动就报错**
（不静默降级 —— 那会让部署的人以为连的是 https，而音频在明文上传）。
客户端：配对时**取回服务端证书并固定**（`credentials.cert_pem`），之后每次调用只认那一张，
于是**不用给每台机器装自签根**（设计 §7.5 ① 说的那件事，现在真的成立了）。

三条边界写在设计里：不传 `fp=` 时是 **TOFU**（第一次连接本身就是信任建立的那一刻）；
传了就一定要对上，对不上直接拒；**https 而没有固定证书 → 拒绝连接**，
**绝不静默跳过校验**（静默跳过的坏处不是"不安全"，而是从现象上看不出来 —— 照样能用）。

用例 18 条（真 https 握手 13 + 服务端配置 5）。证书是**内嵌的测试专用自签证书**
（`tests/tls_test_cert.py`，CN=echo-tls-test、只签 localhost），所以门禁**不需要
openssl、也不需要 cryptography** —— 不为一个 TLS 用例给门禁加依赖。

写的时候被用例抓到一个真漏洞：**`fetch_token` 忘了用固定的证书** ——
自签证书下换令牌那条路会整个不通，而"图省事把校验关掉"就成了能用但中间人随便进。


### 2.10 审计表与 metrics（2026-09-24，施工顺序 step 4 的后半）

`server/calls.py` + `calls` 表（设计 §7.3 / §8.5 的十列，**一个不多**）+ 两个只读命令。

| 出口 | 数据从哪来 | 为什么 |
|---|---|---|
| `/v1/health` 的 `metrics` | **进程内计数** | 探针每几秒被敲一次，**不能查库** |
| `--stats` / `--list-calls`（管理面将来用同一份查询） | `calls` 表 | 要跨时间、跨客户端，还得重启后仍在 |

三条实现语义：**异步写**（请求路径上只做一次 `put_nowait`，后台线程攒批；
队列满了**丢并计数**，`/v1/health` 看得见）、**只记已鉴权的调用**（401 是扫描器噪音；
探针端点压根不进表 —— 客户端每 30 秒拉一次 capabilities）、**保留 30 天**
（`calls.retention_days`，凭据长期留、调用记录可以老死）。

用例逼出两个真问题：

1. **`insert_calls` 缺字段会插成 `None`**，撞 `NOT NULL` → `IntegrityError` → **整批记录被静默丢掉**
   （调用方是后台线程，只在日志里留一句"写库失败"）。现在缺字段按列类型给默认值。
   抓它的是"只传两个字段"的那条用例。
2. **`_AppCase` 从来没隔离鉴权库** —— `_cfg()` 不设 `auth.db`，于是每个 app 级用例的 lifespan
   都在开 **`{ECHO}/data/server-state/echo-server-auth.db`，也就是开发机上真实那一个**。
   之前没显形只是因为那些用例只读不写；`calls` 一开始往库里落记录，立刻就暴露了
   （跨用例累积 → "只查自己那条"的断言全红）。已在**唯一的配置入口** `_cfg()` 上修
   （与 AGENTS.md 里那几条"测试去动真实状态"的事故是同一个形状）。
   **顺带结论**：以后凡是"新增一张会被写的表"，第一件事是确认测试库是隔离的。



那轮要把剩余功能做完时，逐条查了消费方，发现这两项在**当前架构里没有位置**。
判据不是"难不难"，是"**做完谁用**"：

| 项 | 为什么不做的证据 |
|---|---|
| 服务端 `POST /v1/tts` | 客户端**已经拍过板**：`router.DEFAULT_ORDER` 里 `"tts": ()`，注释"不走能力层（客户端自己的 providers 管）"。而且合成一句话在客户端是毫秒级本地活，走服务端变成一次往返；真做还要在 GPU 机器上选一套 TTS 权重（现在一套都没有，也没测试环境验音质） |
| `mode=turns`（逐 turn 嵌入） | `diarize.turn_embeddings` 在客户端**一个消费方都没有**：全仓只有 `base.py`（槽清单）、`router.py`（顺序 + 设置映射）、`capability_admin.py`（中文标签）提到它。服务端现在是**如实拒绝**（400 + "v1 只支持 segment"），不是假成功 |

**什么时候该做**：出现"一台没有任何可用音色的客户端"（TTS）或"客户端需要逐句说话人向量"（turns）。
在那之前，做出来就是纯负债 —— 要跟着协议/鉴权/配额/文档/测试一起演进，却没有任何东西变好。


### 2.4 管理动作（2026-09-24 补齐到命令行）

`server/main.py` 的命令行入口现在齐了，**运维不需要网页也能干活**：

```bash
--new-client "张三的办公本" --scopes "asr diarize"   # 新建 = 发一张带名字与 scope 的码
--new-pairing-code                                 # 只发码（名字/scope 由对端自报）
--list-clients / --list-codes / --show-client ID   # 看
--revoke ID                                        # 撤销（版本号 +1，立即失效）
--disable ID / --enable ID                          # 禁用（403）/ 启用（原令牌直接能用）
--set-scopes ID --scopes "asr"                      # 改权限，**下一个请求**就生效
--rotate-secret ID                                  # 换 secret；旧令牌与旧 secret 立刻全失效
--set-quota ID --daily-audio-minutes 120            # 每日音频上限（0 = 用全局默认；不清零已用量）
```

三条容易搞错的语义（都已写进用例）：

1. **名字 / scope 跟着码走**，而且**码上带的盖过对端自报的** ——
   自报的能改名，那份清单就不算清点了。为此 `pairing_codes` 加了 `name` / `scopes` 两列。
2. **`--rotate-secret` 同时 bump `token_version`**：v1 没有宽限期，客户端本来就要重新配对，
   所以不给"旧 JWT 还能再用一小时"的窗口（留着反而让人以为轮换没生效）。
3. **`--enable` 之后不需要重新换令牌**（`disabled` 与 `token_version` 是两回事）。

`CREATE TABLE IF NOT EXISTS` 不会给已存在的表补列，所以加了一段**最小的补列迁移**
（已存在的库自动补上，数据不丢）。再多就得换真正的迁移工具。

### 2.16 真机联调：把整条链在这台机器上跑起来（2026-09-24 晚）

"契约用例全绿"与"一个人真的在面板上点一遍"之间隔着一层，这一晚就是把那层走掉。
链路（全部真跑，不是模拟）：dev ECHO（`18060`）→ 配对 → 选后端 → 会议转写 → 后端记账。

**跑通的三段**（数字都是这台机器上实测的）：

| 段 | 怎么跑的 | 结果 |
|---|---|---|
| 会议转写走 http 后端 | 重转 45.2 s 的会议 | `GET /api/meetings/1` 的 `capability.picks` = `转写文本 → ECHO 后端`；后端 `--stats` = `cli-e4586e /v1/asr 1 次 0.75 分钟 平均 12460 ms` |
| 会议转写走 **https** 后端 | 自签证书 + 配对串里的 `fp=` | 重转 77.4 s 的会议：配对串粘一次拿到 `cli-b4e354`，`/v1/health` 走**固定证书**读到 `totalCalls=1 audioSeconds=77.4 quota=1.29 分钟`；不固定证书时正确 `CERTIFICATE_VERIFY_FAILED` |
| 只读管理面 | `server.admin_listen=127.0.0.1:8901` + `--new-admin ops` | HTML 200；未登录 401、口令错 401、登录后 `overview/models/clients/calls/inventory/me` 全 200；调用记录**跨重启还在**（读的是库） |

**挖出来的八件事**（前四件是"只有真机才会现形"的那一类，后四件是顺着手往下走时
在**同一条链**上撞出来的）：

1. **回环转发把 `context=` 透传给了 `open()`** ——
   `OpenerDirector.open() got an unexpected keyword argument 'context'`，
   现象是"后端明明活着、能力层却一个请求都发不出去"（`capsError`）。
   根因：`context`/`cafile` 是 **`urlopen()` 的形参**（它内部拿去建 `HTTPSHandler`），
   `open()` 不认；而能力层每个请求都带着它（http 时带 `None`），回环那条分支原样透传。
   **为什么 1477 条用例没拦住**：全局转发只在 `app/main.py` 里装，测试里没人一起走
   "装了转发 + 带 context"这条路。补齐：`netlocal._open()` 统一分派（回环把 TLS 形参交给
   opener 承载，外部交回标准库），`tests/test_netlocal.TlsKwargsThroughBypassTests`（5 条，
   含真 https 握手 + 别人的证书必须被拒），并做了一次**变异测试**验证它有牙
   （把 `kwargs.pop` 退回 `kwargs.get` → 4 errors + 1 failure，还原后全绿）。
2. **配对串里的 `fp=` 一直没有出口** —— `pairing.pair(cert_fingerprint=…)` 与它那批
   用例都在，但 `PairBackendIn` 没这个字段、面板也无处可填，于是"防中间人"那一步
   从界面上根本走不到（这正是 §三第一行那种"有没有消费方"的问题，只是发生在参数上）。
   补齐：接口 + `capability_admin.pair()` + 面板（**认整串** `echo://pair?host=…&code=…&fp=…`，
   `parsePairString`）。
3. **服务端打出来的配对串有两处"抄过去连不上"**：不带 scheme（https 后端要 `https://`，
   否则客户端按 http 连）、以及出厂默认监听 `0.0.0.0` 时**把通配地址抄给客户端**。
   现在按"有没有配 TLS"写 scheme；通配地址换成探测到的地址并附一句怎么改。
4. **`capabilityDiarizeBackend` / `capabilityEmbedBackend` 在会议链路上还没有消费方**
   （见 §三第一行）：设置是真的、路由表认识，但会议只对 `asr.text` 问过路由。
   这条**没修**，是 step 4 的正题；写在这里是因为"面板上有的开关"最容易被当成已完成。
5. **门禁里有一条用例依赖"这台机器没配过对"** —— 这台机器真配对一次之后
   `test_not_configured_is_absent_and_says_so` 立刻红了（`refresh()` 返回 True）。
   根因不是一个测试写漏了打桩，而是 `EchoServerClient(creds=False)` **名不副实**：
   注释说"就当本机没配对"，实际只挡住了 `self._creds`，地址照旧从凭据文件读出来。
   现在是**地址也不看凭据**（`_base_url_from_settings(use_credentials=…)`），
   并加了一条用例把两种读法都钉住（默认 = 只配对就能用；`creds=False` = 真的当没配对）。
   —— 与 §2.3 那几条是同一族：**测试读到真实机器状态**，而在开发机上那种状态会变。
6. **退回本机那条路是"静默"的**（读代码时发现，用真机复现）：
   `_capability_asr_session` 里那句"仍走本机引擎 —— 原因：…"只在
   `capabilityEchoServerUrl` 非空时打，而"只配对、什么都没配"从 §2.7 起就是能用状态 →
   那种机器掉了后端**一句话都不留**。改成问 `echo_server.configured()`（设置或配对任一个）。
   真机复现：`privacy=none` → 日志里出现
   `配了能力后端，但本场仍走本机引擎 —— 原因：echo-server(blocked)；-(absent)`，
   这一场确实由本机转出 8 行。
   顺带把**语义**记清楚：配额用完（后端**调用时**才拒）**不会**退回本机 ——
   那一场是 `status=error` 且 0 行，日志写明 `reason=quota code=quota_exceeded`；
   退回本机只发生在**计划阶段**就用不上后端的时候。
7. **详情页会把上一场的路由结论显示成本场的** —— 真机现场抓到：`meta.json` 是同一场会
   反复重转时**覆盖写**的，而 `capability` / `timestampsKinds` 只在走能力层时才写。
   于是"上一次走 ECHO 后端、这一次退回本机"之后，页面上那行还是上一次的
   `转写文本 → ECHO 后端`。现在**没走能力层就把这两项删掉**（`_apply_capability_meta`），
   面板那块随之隐藏 —— `web/meeting.html` 自己那句注释是对的："页签说会走 GPU、
   实际走了本机"这种对不上，比不显示更糟。
   真机验证：`privacy=none` 重转后 `meta.json` 只剩
   `config/durationSeconds/end/segments/start/transcribed`，详情页的 `capability` 变成"没有"。
8. **`--set-quota` 的提示与实测不符**：原话是"上限的**下一个请求**就生效"，实测改完立刻打
   仍然 200，要等**鉴权缓存**（默认 60 s）过期才是 `429 quota_exceeded`
   （`Retry-After: 4648`，指到本地 0 点 ✓）。提示已改成"最多 N 秒后生效（要它马上生效就
   重启服务端）"，并加了一条用例禁止它再说"下一个请求就生效"。

### 2.18 同事《3.0 测试报告（0014 包）》的七个问题（2026-09-25 凌晨，全部落地）

同事在**一台干净 Windows**（无 D 盘、无 uv/py、走 python.org 嵌入包、ExecutionPolicy=Restricted）
上装 `ECHO-kit-20260925-0014`：**装成功、EXIT=0、新布局与 L1 都按设计落地**
（`C:\ECHO\{echo-core,runtime-core,data,models,dsh\{app,home},meeting,aide}`、
L1 七个重依赖全 ImportError、常驻 468.8 MB）。但"全新安装"这条路被三连 P0 挡着。
七个问题**都已修**，每条补了用例（能用变异测试证有牙的都证了）：

| # | 问题 | 根因 | 修法 |
|---|---|---|---|
| **A** | **服务起不来**：`No module named 'app'` | `install.ps1` 造嵌入包时往 `python311._pth` **写死 `..`**（扁平布局的相对项）。3.0 代码在 `<base>\echo-core`，`..` 只到 `<base>`；而嵌入包是 **isolated 模式**，`PYTHONPATH` 被完全忽略 → 没有兜底 | 相对项按 `Resolve-CoreDir` 算：新布局 `..\echo-core`、扁平 `..` |
| **B** | **模型必然下不下来** | `$ENGINE_MAP['sherpa'].pip` 只有 `sherpa-onnx`，**没带下载客户端**；而 `modelinfo._snapshot()` 是"先 ModelScope、失败再 HF"，一个都没有就必失败。whisper-* 顺带带 hf-hub、sensevoice/qwen3asr 顺带带 modelscope —— **默认档是第一个"只有 sherpa"的档位**，这个洞一直被盖着 | sherpa 档加 `modelscope`（同事那台实测 189 MB / 15 秒）；`requirements-core.txt` 与 `profiles.json.require` 同步加（它是**运行时**依赖）；mac 侧 `.sh` 同步 |
| **D** | 桌面快捷方式报 `venv missing` | `launch-desktop.ps1` 仍只查 `$root`（= `echo-core`） | 加 `$installBase = (Get-EchoRoots -Start $root).Base`，运行时/端口/日志改用它（我这侧 `e0725467`，与同事的补丁同形） |
| **F** | **只装标准版时 ECHO AUTO 100% 注册不上**（沿用 2.0） | `llm_router.dsh_homes()` 判据只认 `settings.yaml`，而**标准版 harness 只写 `.credentials.yaml`** → 家目录列表恒空 → `sync()` 报"没找到 DSH 家目录"；`boot.py` 的 15s×8 次重试救不了（判据本身不可能满足） | 判据放宽成"`settings.yaml` **或** `.credentials.yaml`"；两样都没有仍然不碰（D25 不变） |
| **C** | `Wait-Model` **空等 1800 秒** | 读 `/api/models` 的 `jobs.<id>`，真实形状是 `jobs.items.<id>`（`modelinfo.jobs()` 返回 `{active, items}`）→ `$job` 恒 null → 状态归一成 `queued` → 等到超时。症状是"脚本卡住了" | 改读 `.items`；并在用例里同时钉住接口侧的形状 |
| **E** | `startup.ps1` / `restart-echo.ps1` 同类 `$root` 误用 | 头部算过 `$base` 但下面漏改：`Resolve-EchoPort $root`、`Test-EchoAlive` 的 `echo.pid`、`restart.log` | 全部改用 `$base`（`restart-echo.ps1` 不 source launch-lib，按同规则内联算） |
| **G** | 安装日志中文**双重编码**（`ç¬ç«`） | `Invoke-Api` 用 `Invoke-RestMethod`，而 PS 5.1 对**不带 charset** 的 `application/json` 按 ISO-8859-1 解 | 改成 `Invoke-WebRequest` + 自己 `UTF8.GetString($resp.RawContentStream)`；请求体也显式 UTF-8 |

**新用例**：`test_install_entry.DownloadClientTests`（4 条）、
`test_install_state.ComponentsScriptReadsTheApiRightTests`（3 条）、
`test_dsh_multi_home.test_a_home_with_only_credentials_is_a_home_too`。
A 与 B 的护栏做过**变异测试**（把修复退回去 → 用例变红 → 还原 → 变绿）。

**报告里"不是包的问题"的两条也值得记**（同事自己判定的，我认同）：
`koffi` 的预编译自检在那个执行环境里必失败（沙箱拦 node 同步子进程接口 → 退化源码编译 →
无 CMake → `npm install` 整体回滚），绕法是 `--ignore-scripts` 装树再定向 `npm rebuild`；
以及**沙箱每 2~4 分钟回收后台进程树** —— "页面是黑的"真因是服务被杀，不是前端渲染
（他用无头 Edge 截图证明了页面是好的）。

> **教训（值得留在显眼处）**：这三个 P0 都是"**某个形态第一次成为唯一形态**"才现形的 ——
> 只有 sherpa 时没人顺带装下载客户端；只有标准版时没人满足家目录判据；
> 而新布局把"代码根 = 数据根 = 安装根"这个隐含前提一次打破了**三处**（`_pth`、启动器、技能脚本）。
> 以后每加一个新档位 / 新布局，先问一句：**它是不是第一个这样的？**

### 2.17 安装根布局（2026-09-24 晚，用户拍板；**代码侧已落地，安装侧还没做**）

用户定稿的形状（`3.0-设计总览与组件关系.md` §2.2 那份 9-23 提案的三处更正：
第四个目录叫 **`aide`**、`dsh/` 里分 `app` 与 `home`、`meeting`/`aide` 同时是数据目录与工作区）：

```
{echoBase}/
├── echo-core/   代码（升级整体覆盖）
├── data/        echo.db / logs / pid / token
├── models/      模型权重
├── dsh/{app,home}/   本体 + DSH_HOME（skills/会话/settings）
├── meeting/     会议数据 ≡「会议空间」工作区
└── aide/        指令数据 ≡「指令空间」工作区
```

**判据是"布局自描述"**（`paths.echo_base()`）：代码目录名叫 `echo-core` → 父目录就是安装根；
否则（开发树、老装机）= 空串，**每个 `*_root()` 逐字回落到 2.0 的路径**，一个字节都不搬。
所以老装机零迁移、也不需要新的配置键（安装根配错的后果是全盘静默跑偏，不值得开一个面板项）。

**已落地（代码侧）**：`paths.py`（`echo_base()`/`base_dir()`/`dsh_root()`/`dsh_home_root()`/
`meeting_space_root()`/`command_root()` + `{ECHO_BASE}` 占位符 + 体检页多列四个根）、
`workspaces.py`（目录只从路径层来）、`harness_proc.py`（本体两个落点都探、家目录跟随）、
`meeting.py` 与 `agents/dsh_agent.py`（不再直接读那两个设置串）、`config.py`（出厂值）、
`modelinfo.py`（**HF_HOME 兜底值改成 `models_root()`** —— 见下）、各处文案。
用例：新增 `tests/test_install_base_layout.py`（19 条，含 L6"六类东西都不许进 echo-core"
与"HF_HOME 必须等于 models_root()"两条硬断言）+ 改了 6 处"必须在 {ECHO} 下"的旧断言。

**清点时排第一的坑（已修）**：`modelinfo.py` 原来写死 `HF_HOME = {ECHO}/models`，而
`main.py` 只在用户显式配了 `modelsDir` 时才覆盖 —— 新布局下 `{ECHO}` 是 `echo-core`，
于是**权重下进代码目录、`models_root()` 去 `{echoBase}/models` 找**：huggingface 会静默
重下几 GB，而且模型落在"升级即覆盖"的目录里（违反 L6）。

**还没做（按风险排序；2026-09-25 凌晨更新过一轮状态）**：

1. **安装侧**：`scripts/install.ps1` ✅（全新安装把代码放进 `<root>\echo-core`、建六个兄弟目录、
   运行时/数据落在安装根；**已有扁平安装原地升级不动代码** —— 判据见 `Resolve-CoreDir`，
   三条分支都 dry-run 验过）。
   **启动/运维脚本**：`start.ps1` / `startup.ps1` / `stop.ps1` / `launch-desktop.ps1` /
   `restart-echo.ps1` / `echo-instance-lib.ps1` ✅（都按 `$base` 拆开了"代码根 vs 数据根"，
   含 pid/端口文件）；技能的两个 components 脚本 ✅；**mac 侧四个脚本** ✅
   （`echo-install-components.sh` / `setup_mac.sh` / `start_mac.sh` / `stop_mac.sh`，
   护栏 `tests/test_install_entry.MacInstallBaseLayoutTests` 4 条 —— 它当场抓出我自己两处漏改）。
   **仍未做**：`deploy-stable.ps1` —— 它只认"仓库根 + DestDir"，往**扁平**稳定版覆盖是对的，
   但哪天往新布局的目标覆盖代码，`Copy-Item <kit>\ECHO\* → DestDir` 会拷错一层（要改成
   `DestDir\echo-core`）。目前稳定版是扁平的，所以**没有实际影响**，记在这里免得忘。
   ⚠️ 改 `.ps1` 的两条编码纪律（本轮各踩一次）：含中文的**必须带 BOM**（`edit` 工具会抹掉，
   PS 5.1 随后按 ANSI 解析 → 几十处"意外的标记}"）；**ASCII-only 的文件（无 BOM）注释必须用英文**
   （`startup.ps1` / `stop.ps1` / `echo-instance-lib.ps1` 都是这一类）。两者都由
   `tests/test_script_encoding.py` 守着，改完顺手核一遍。
2. **两个待拍板的点 —— 都已定并落地**：
   * **venv 放哪**：定为 `{echoBase}/venv`（既不是代码也不是用户数据）；`runtime-core` 同理。
     `install.ps1` 与启动脚本都按这个落点写好了。
   * **agent 的默认 cwd**：定为「指令空间」。`agents/base` 的常量 `ECHO_WORKSPACE` 换成函数
     `echo_workspace()`（→ `paths.command_root()`），三个消费方 + `app/dsh.py` 的再导出都改完，
     护栏见 `tests/test_install_base_layout.test_the_agent_fallback_cwd_is_the_command_space`。
3. **`dsh-failover/config.json` 与 `homes.json` 住在代码目录里**（用户的模型路由配置 +
   热读清单，打包器刻意不打它们）：新布局下"删掉 echo-core 重装就丢配置"。L6 要一个落点。
4. 交付包（`build_kit.py` 出的 kit 目前仍是"代码在包根"的老形状）、老装机迁移表、
   相关文档（`DEPLOY.md`、技能 `SKILL.md`、试用手册）。

## 四、下一步：施工顺序（能力路由文档 §11）

| 阶段 | 内容 | 状态 |
|---|---|---|
| **-1** | 交付包拆「默认档 / 离线增强档」（纯配置 + 依赖清单） | ✅ **已落地**（`components/profiles.json` + 15 条护栏，见 §2.15）。**真机装完的体积/耗时实测仍待办**（要干净机器） |
| **0** | 定协议 + 抽 `CapabilityClient`；本地实现原样保留 | ✅ **已完成**（`app/capabilities/`，见 §2.5） |
| **1** | 建 ECHO 能力后端（只包槽，无 DB、无业务词）+ 客户端 `echo-server` 后端 | ✅ **已完成**（`server/` + `echo_server.py` + **配对/令牌/凭据**，见 §2.6） |
| **2** | 加 `intranet` 适配器 + 能力探测 + 熔断 | ⏳ 卡在 §8.2.2 那三件事上（协议要先问同事；见 `能力路由` §8.2 的更正） |
| **3** | 能力路由表 + 路由设置卡 + 执行计划落 `meta.json` | ✅ **已完成**（§2.7）；**面板也把它显示出来了** —— 会议详情页顶部那一行（`web/meeting.html:renderCapability`，数据来自 `GET /api/meetings/{id}` 的 `capability`），2026-09-24 真机看到真数据（§2.16） |
| **4** | 向量空间字段迁移 + 会议级锁定 + 校准按钮 | ◐ 路由层已强制同源；**落库字段与会议侧接入还没做**（见 §三第一行那条：会议链路的 `diarize.*` / `speaker.embed` 还没有消费方） |

**下一步只剩一条路要走**（别的都通了）：

> **把已经记下来的执行计划显示出来：会议详情页回答"这场会用了哪个后端、有没有降级过"。**

**先纠正一处我自己写错的判断**（2026-09-24 晚查代码时发现）：上面原来写着
"只有结论没有过程、`skipped` 没留下" —— **那是错的**。`meta["capability"]` 存的就是
`Plan.as_dict()`，而它里面本来就有 `picks` / `skipped` / `candidates` / `notes`
（`meeting.py:793` 写，`router.py:156` 出）。所以**数据已经全了**，
缺的只是"没人把它显示出来"：

* 会议详情接口（`GET /api/meetings/{id}` → `meeting.get_meeting_detail()`）
  **完全不读 `meta.json`**，所以面板连 `meta["capability"]` 都拿不到；
* 面板那边也没有任何地方渲染它 —— 排查时唯一的出口还是翻日志文件。

要做的三件事：
1. 会议详情带上 `meta`（至少 `capability` 与 `timestampsKinds`）；
2. 降级原因（权威十词）译成人话 —— **放在 `capability_admin`**（槽/后端/原因的中文名
   已经在那里了，别在面板里再写一份词汇表）；
3. 显示位置：会议详情页（`web/meeting.html`），**不是**「能力」页签那张卡 ——
   那张卡讲"现在配的是什么"，这里讲"上一场实际发生了什么"，是两件事。

**已经不用再做了的**（原来列在这里、现在完成了）：面板那一页（§2.7）、
客户端凭据管理（§2.6）。

## 五、怎么测（冷启动者先看这个）

```bash
# 起本机后端（配置在 gitignore 的 data/ 下，用本机已有的 sensevoice 模型）
python -m server.main --config data/backend-dev/server.yaml

# 一条命令自测 21 项
python scripts/smoke-echo-backend.py

# 单测 / 门禁
python -m unittest tests.test_server_contract
powershell -File scripts/check-windows.ps1
```

细节与"看起来像坏了其实是设计"的对照表见 **`server/README.md`**。
设计理由见 `docs/ECHO能力后端-服务端设计.md`（§12 有一张
"断言 → 状态 → 落在哪条用例"的清单，**没实现的都标了 ⏳**）。

### 2.19 会议链路接上能力路由（施工顺序 step 4）+ 交付包重出（2026-09-25 凌晨）

**step 4 落地了**：会议的说话人分离/声纹不再直接调本机 `app/audio/diarize.py`，而是走
`router.call()` 的能力层。核心改动三处：

* `app/capabilities/router.py`：`auto` 链里**去掉本机** —— `diarize.turns` / `diarize.embeddings` /
  `speaker.embed` 变成 **ECHO 后端唯一**（`asr.timestamps` 同样去掉本机）。理由是**向量空间**：
  本机 `diarize.*` 一旦启用就引入第二个 `vectorSpaceId`，混用的表现是"认错人且不报错"
  （§5.1 / §5.1.1）。**"不默认本机" ≠ "禁止本机"**：用户显式把槽设成 `local` 仍然照用 ——
  那是他选的主选，不是兜底。
* `app/meeting.py`：把原来只服务 `asr.text` 的会话判据抽成 `_session_slots()` + 会话对象。
  槽的顺序有讲究：说话人那一族按 `diarize.turns` → `speaker.embed`，**向量空间锁（L5）由前者定**；
  `speaker.embed` 只在声纹识别真的会用时才列进来（否则面板上会多一行并不存在的跳过原因）。
* `app/config.py`：`asr.text` 那句默认链描述改成与代码一致（原来写的是"内网公共 → ECHO 后端 → 本机"，
  与 `router.DEFAULT_ORDER` 不符 —— 这是**过期文案**，不是行为改动）。

验证：`tests/test_meeting_capability` 30 条 OK；全量门禁 **5/5 PASS、1546 条用例**（比上一轮 +14）、
exit 0。这一步的实现由子代理完成（我给它写了三条底线：会议槽无本机兜底、老装机行为逐字不变、
降级要留痕且不残留上一场的执行计划），我抽查了 diff 的关键三处并跑了闸。

⚠️ **留给用户拍一下的一处**（我没动行为）：`router.DEFAULT_ORDER` 里会议 `asr.text` 是
**ECHO 后端 → 本机 → 内网公共**，而 `能力路由` §5.1 的表写的是 **内网公共 → ECHO 后端**。
这次只把**描述**对齐到代码。哪个是最终意图，需要你定（要改行为的话是一行的事）。

**交付包重出**：`dist/ECHO-kit-20260925-0300.zip`（3.53 MB，`git=01859ad6`，`[ok]` 自检）。
逐条核对过：报告的 A–G 七条修复、step 4、mac 四个脚本、`echo_workspace()` 都在包里 ——
**上一个包（0014）不含这些**，测试机要重装就用这个。

**仍然没做的**（如实列，别读成已完成）：
1. `dsh-failover/config.json` 与 `homes.json` 仍住在**代码目录**里（用户的模型路由配置 +
   热读清单）：新布局下"删掉 echo-core 重装就丢配置"。L6 要一个明确落点（数据根或 `dsh/home`）。
2. 老装机迁移表（把扁平装机搬到六目录布局）—— 一直没做，因为**没有任何老装机需要搬**：
   判据是自描述的，扁平树继续按扁平跑。
3. 真机复测：冷启动耗时（同事实测沙箱里 36 秒 / 设计目标 <10 秒）、默认档体积与常驻内存，
   要在无进程回收的机器上量。
4. 内网公共 ASR 适配器（step 2）仍卡在同事那边的协议三问。


### 2.20 qwen3asr 后端变慢的根因与修复（2026-09-26 凌晨，实测）

用户把后端的 ASR 从 `sensevoice` 换回出货清单的 **qwen3asr**（`server/engines.py:default_specs()` 那套：
`asr-long` / `qwen3-asr-0.6b` / `supports: [asr.text, asr.timestamps]`）后，发现**10 分钟音频要 4~5 分钟**，
且面板没有句级时间戳。排查结论（都有原始数据）：

**① 慢的真因是"整段喂"把显存撑爆，不是模型或设备配错。**
```
整段 600s 直接喂: {'extract_feat': 0.0, 'forward': '255.821', 'batch_size': '1', 'rtf': '1.000'}
                  peak_cuda_mb: 11399 MB  ← 超过这张 8151 MB 的卡 → 溢出到 Windows 共享内存、边换页边算
                  text_len: 35            ← 而且文本是坏的（10 分钟只出 35 个字）
分块 60s + 批 4 : forward 35.1 s / peak 3994 MB / text_len 3531  ← 文本正确、显存减半
```
GPU 侧证据：`device: cuda:0`、`dtype: bf16`、推理中 `util_mean 95.4%` —— **不是"没用上 GPU"**，
所以"换个设备就能快"这条路不存在；能改的是**喂法**。

**② 没有时间戳是接线缺口，不是缺权重。** 日志里那句
`return_time_stamps requires forced_aligner. Skipping timestamps.` 的原因不是权重缺失 ——
`Qwen--Qwen3-ForcedAligner-0.6B`（1.75 GB）**本机 modelscope 缓存与 `models/hub` 里都有**，
是服务端那条加载路径**没把 `forced_aligner` 传进去**。

**③ 修复把参数提到共用位置**（本地与后端同一套，`app/audio/stt.py`）：
```
QWEN3_CHUNK_S = 60.0
QWEN3_BATCH   = 4
_get_qwen3asr(..., forced_aligner=QWEN3_FORCED_ALIGNER)   # 默认接上对齐器
```
顺带解释了用户"单机版当年没这么慢"的印象：**客户端那条路本来就有分块参数，后端的加载路径没有** ——
同一个模型、一边分块一边不分块。

**④ 实测对照（同一段 600 秒真会议音频 `data/meetings/2026-09-20_10-48-26/01.wav`，同一权重、`cuda:0`/`bf16`）**

| 路径 | 墙钟（多次） | RTF | 文本 | 句级时间戳 |
|---|---|---|---|---|
| **本地 A**（客户端 `_qwen3asr_sentences`） | 40.2 / 36.4 / **33.2** s（中位 **36.4**） | 0.055–0.067 | 3508 字 | **205 句 ✓** |
| **后端 B**（`POST /v1/asr`，https+JWT） | 63.8（首次含预热）/ 42.1 / 46.9 / **44.4** s | 0.070–0.078 | 3508 字 | **205 句 ✓** |
| 后端·修复前（整段喂） | **236.6 s** | 0.394 | **35 字（坏的）** | 无 |
| 参照：SenseVoice（同一后端，修复前） | 0.16 s | ~0.003 | 有 | **无**（`supports` 只有 `asr.text`） |

**两条路的产出逐字节相同**（`ab-a.text` 与 `ab-b.text` 的 sha256 前 16 位同为 `84665722809b3919`；
`ab-a.sentences.json` 与 `ab-b.sentences.json` 同为 `6febaf250f175b82`，205 条逐条相同）——
也就是说"本地 vs 后端"如今**只差速度**：后端 **236.6 s → 44.4 s（快 5.3 倍）**，剩下的差距是
19 MB 音频上传 + 服务端落临时文件 + 首次预热。

**⑤ 参数定案**：后端用 `chunk 60 s + batch 4 + forced_aligner`（即出货清单那套 + 共用分块参数）。
`est_vram_mb: 3900` 是**按 24 GB 卡估的占位值**（`engines.py` 自己写明"上线前要用真机压测校准"），
本机 8 GB 卡上峰值约 4 GB，安全；但如果将来与 `diarize`（2600 MB）同时常驻仍需盯。

**⑥ 踩到的两个环境坑（都不是 ECHO 的 bug，但会让人误判）**
* **`schtasks /end` 不会杀子进程**：python 变孤儿继续占着 8900，于是"重启"后仍是**旧进程 + 旧配置** ——
  表现就是"配置改了却没生效"。正确做法：`Get-NetTCPConnection -LocalPort 8900 → OwningProcess → Stop-Process` 再起。
* **PowerShell here-string 里的反引号是转义符**：用它写含反引号的 YAML 注释会被拆行、直接把配置写坏。
  教训：**配置改动一律"Python 写 + `yaml.safe_load` 校验通过才落盘 + 先备份"**（本次靠校验拦住了一次）。



### 2.21 显存记账校准（2026-09-26 上午，dev 配置）

§2.20 那次实测暴露了一个**参数层**的坑：出厂占位值 `est_vram_mb: 3900` 比实测峰值（4.6 GB）小，
而 `vram_budget_mb: 8000` 又比这张卡的**可用**显存大 —— 于是池认为 `asr(3900) + diarize(2600) = 6500`
装得下，两个模型同时常驻，实际整卡跑到 **7.2 / 8.15 GB**，同一段推理从干净的 36–40 s 掉到 **62–74 s**、
GPU 利用率从 54–60% 掉到 **24–32%**（同一机制的小一号复现，见 §2.20）。

改法（只改 `data/backend-dev/server.yaml`，**它是 gitignored 的本机配置**，不进仓库）：
```yaml
server:
  vram_budget_mb: 7000        # was 8000 —— 让它 < asr(4700)+diarize(2600)=7300
models:
  specs:
    - id: asr-long
      est_vram_mb: 4700       # was 3900（出厂占位值）—— 按实测峰值记账
```
依据与权衡：
* `pool.py:311` 的语义是**预算不足时驱逐 LRU 模型**（不是拒绝加载），所以收紧预算会让
  "ASR 与说话人分离"**改为 LRU 串行**，各跑各的干净速度；两者单独都装得下（4700 / 2600 ≤ 7000）。
* **本机不给 ASR 设 `resident: true`**：常驻模型**不可驱逐**（`pool.py:47` 注释"不参与 LRU"），
  8 GB 卡上会让 `diarize` 加载失败。**≥12 GB 的卡上可以反过来**：保留实测 `est_vram_mb` +
  `resident: true`（启动预热，省掉首次请求的 30–60 秒）。
* 只改数值、不插注释 —— 上一版试图顺手插注释时**把 YAML 写坏被校验拦下**（`server:`/`tls:` 结构处），
  这再次说明"**Python 改 + `yaml.safe_load` 校验后才落盘 + 先备份**"这条纪律是必要的。

真实调用表里的对照（`--stats`，同一台后端）：
```
cli-98c3df   /v1/asr  16 次  音频 129.90 分钟  平均 50630 ms  p95 206320 ms  最慢 284873 ms   ← 修复前（整段喂）
cli-50aa69   /v1/asr   9 次  音频  90.00 分钟  平均 44460 ms  p95  63724 ms  最慢  74293 ms   ← 修复后（分块+批+对齐器）
```


---

## 六、2026-09-26：设置信息架构（IA）重构 + 本地模型使用追踪与清理（本轮）

用户给的新前提（原话见交付报告）：配置分**三类**，第四个位置留给历史 ——

1. **ECHO 通用**：快捷键 · 打开/启动形式（桌面/托盘/开机自启）· 运行状态（服务/端口/版本/重启停止）
2. **业务配置**：会议（录音设备 · 分段 · 保存策略 · **转写走哪条路**）· 语音指令（唤醒/采集/TTS/反馈）· 队列 · 工作区与归档
3. **能力与智能体**：智能体选择 · 模型路由（合并卡）· 设备选择（池与优先级）
   ├ 已配对后端连接情况 · ├ 本地能力部署运行情况 · └ 清理
4. **历史**：指令历史 · 会议历史 —— **这一轮只预留位置/空态**（没写历史功能）

做法与边界（细节与证据见本轮交付报告）：

* 顶层页签 7 → 6（`仪表盘 / ECHO 通用 / 业务配置 / 能力与智能体 / 历史 / 会议记录`）；
  旧深链 `?view=voice|agent|settings|boot|failover|capabilities|wizard` 由 `VIEW_ALIASES` 折算。
  （**第二轮又 6 → 5**：`会议记录` 并进 `历史 → 会议历史`，见 §七。）
* **默认折叠**：只有"配置为要用的/当前生效的"默认展开；whisper 三档（tiny/base/small，
  权重在本机但已退役）落在「其余本地引擎」里默认收起，点开才见；启动日志 / 运行环境 /
  已配对后端 / 清理 / 环境体检也都默认收起。
* **一个实体只有一处状态**：会议转写/分离/声纹"由谁做"从「模型路由 → 会议能力通道」
  挪进「业务配置 → 会议」（`#rtCapHost` 撤销）；播放设备池只留在「设备选择」。
* **新增 `model_usage`（schema v7）**：模型 id + 上次使用 + 次数 + 「保留」钉子，
  在**真的加载/调用**时写（`stt.transcribe_ex` / `load_engine` / `diarize`），`/api/models` 下发。
* **清理**（`/api/models/cleanup/preview` + `POST /api/models/cleanup` + `/api/models/pin`）：
  默认只建议、先预览后确认、默认 90 天（`modelCleanupDays` 可配）、允许打钉子；
  **绝不删**在用/当前配置选中/被依赖组合（qwen3asr = ASR + 强制对齐器，同生共死）；
  删除覆盖 `models\` 与 `~/.cache/modelscope/models` 两处缓存，如实回报释放字节与失败原因。
* **两个小修**：`stt._whisper_dir()` 认 ModelScope/HF 缓存（此前"面板说已就绪、引擎却静默重下
  461 MB"）；`tests/__init__.py` 的退出收尾改成**只清本进程登记过的目录**（并发跑测试时
  互相删临时目录 → `no such table: settings`）。
* 交付截图：`docs/面板-IA-1-ECHO通用.png` … `docs/面板-IA-5-历史占位.png`；
  设置项归属表由 `scripts/gen-settings-map.py` 重新生成（108 项，`--check` 通过）。

## 七、2026-09-26（第二轮）：把「历史」从占位做成真的两页，并把「会议记录」并进去

用户早前定的"后面再续上指令和会议历史"到这一轮兑现。**顶层页签 6 → 5**。

* **「会议记录」整体并入「历史 → 会议历史」**（同一个实体不该有两个页签）：
  会议列表**只有一个宿主 `#meetingList`、一个渲染函数 `renderMeetingItems()`**
  （仪表盘那张"最近几场"小卡也用它，`opts.full` 决定画不画历史页额外那几格），
  状态只有一个来源（`GET /api/meetings`）。`#view-meetings` 与页签整段删除；
  老深链 `?view=meetings` 由 `VIEW_ALIASES` 折算到历史页，并自动打开第二个子页签
  （`_bootWantHist`；`data-goto="meetings"` 走同一条路）。
* **历史页是两个子页签**（复用会议详情页那套 `.tabs-sm/.tab-sm`，不新造控件）。
  整页不滚、列表自己滚：`body.on-history { height:100vh; overflow-y:hidden }` +
  穿过子页签那一层的 flex 高度链（`min-height:100vh` 只是下界，不钉高度的话
  126 条会把整页撑到 6000px，「加载更多」等于没有）。
* **指令历史**：什么时候说的 / 说了什么 / 走哪条链路与后端 / 耗时 / 成败（失败给真原因）/
  系统回了什么；**按关键词 + 时间过滤 + 分页**，两者都在服务端
  （`GET /api/commands?q=&since=&limit=&offset=`，`db._command_where()` 是唯一判据，
  `%`/`_` 做 LIKE 转义），一次 50 条，「加载更多」按 offset 追加；
  两种空态分开写（"从来没说过" vs "被筛掉"）。
  新增 `commands.meta.backend` = **发送当时**的智能体显示名（老记录没有 → 不显示，不编）。
* **会议历史**：时间 / 时长 / 段数 / 状态 / **转写档位** / **说话人** / **是否已压缩** /
  「看转写 ›」（直达详情页转写页签，`meeting.html` 新增 `?tab=`）。
  转写档位走 `capability_admin.timestamps_summary()`（与详情页同一份翻译、同一份快照，
  会议列表新增 `timestamps` 字段）；说话人由 `db.speaker_names_by_meeting()`
  **一次查询**喂整页；导出沿用详情页的「⬇ 离线HTML」，不另造一个。
  空态说清"点开始录音录一场 / 或用导入录音"。
* **操作入口一个没丢**：开始/停止录音、导入录音、压缩、清理短录音仍在会议历史标题栏，
  重新转写 / 导出 / 纪要 / 归档仍在详情页（用例逐项钉住）。
* 交付截图：`docs/历史-1-指令历史.png` … `docs/历史-6-展开态-压缩面板.png`
  （假数据在 `%TEMP%` 的隔离实例里造，真实 9 场会议只读）。
