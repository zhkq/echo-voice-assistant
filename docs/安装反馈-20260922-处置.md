# ECHO 安装反馈处置（同事 2026-09-21~22 那份）

> 来源：`ECHO-安装反馈-20260922.md`（同事在 Windows 11 / 无管理员 / 有系统代理 / 只有托管式 node
> 的机器上，用 `echo-install` 技能装 ECHO 的全过程记录 + 手工补丁）。
> 本文把每一条对到**当前代码状态**：已修 / 已由他人修 / 待办，并给出证据与做法。
>
> ⚠️ **先说一件要紧的事**：同事手上那份材料是**旧版**。反馈里他自己当作"已解决（手工绕过）"的
> 两条，仓库里已经正式修了 —— 见 A2 / A5 与"说话人分离"。**重新出一份资料夹**（`ECHO/` + 技能）
> 就能消掉其中大半问题。

## 一、处置总表

| 编号 | 反馈要点 | 现状 | 证据 / 说明 |
|---|---|---|---|
| A1 | 技能第 3 步找不到 `install.ps1` | ✅ 已修 | `93e09c9f`（技能让他在 zip 里找）、`79b3821a`（支持"已解开的资料夹"）；现 SKILL.md 写明它在 `ECHO\scripts\` |
| A2 | `Add-Type` 被安全策略（ConstrainedLanguage/WDAC）拦 | ✅ 已修 | `install.ps1` 改用 `tar -tf` + 输出重定向到文件再逐行读；代码注释里记了同事这次事故 |
| A3 | 残留 `echo.db-wal` / `-shm` 导致启动卡在 `Waiting for application startup` | ✅ **本轮修** | 启动时 `db.heal_stale_wal()`：先 `wal_checkpoint(TRUNCATE)` 归位（数据不丢），归位不了就把两个文件改名成 `*.stale-<时间戳>` **留证不删**；一句人话进 `/api/status.startupNotes`，面板只提示一次。放在**单实例锁之后、`db.init()` 之前**（`app/main.py:main()`）。测试 `tests/test_db_wal_heal.py`（7）+ 接线 `tests/test_startup_notes_ui.py`（4） |
| A4 | hf-mirror 证书被代理拦 | ✅ **本轮修** | pyannote 已改 **ModelScope 优先**（`82dbfa4c`）；本轮补上"失败原因透出"：`modelinfo.explain_download_error()` 把裸异常翻成人话（证书 / 代理 / 403 条款 / 401 登录 / 网络），两个源都失败时以 `怎么办：…` 附在后面。测试 `tests/test_model_download_errors.py`（8） |
| A5 | 缺 VC++ 2015-2022 运行库（装 torch/pyannote 必踩） | ✅ 已修 | `ba877dc9`：技能脚本补了**前置检查 + 自动下载安装**（弹 UAC）；失败给手动链接 |
| A6 | 长驻进程被回收、安装不可断点续跑 | 🟡 部分 | 脚本幂等（重跑会跳过已装好的）＋**每步已写独立日志**：两边脚本都把 `say/ok/warn/err/step` 同时追加到 `<安装目录>/data/logs/install-<时间戳>.log`（终端被关掉/被 agent 收走也留痕，写不进去就静默放弃）；**"上次停在哪一步"的提示仍未做**（见 §五） |
| **B1** | 桌面快捷方式启动 → 进程 PATH 无 node → 找不到 npx | ✅ **本轮修** | 见下 §二.3 |
| 观察 | 状态里 `dsh` 显示 offline 易被误读为"坏了" | ✅ **本轮修** | 命名分开：清单里两条 = **DSH Desktop 桌面版** / **标准版 harness（DeepSeek Harness）**；catalog 给 agent 行加 `active`，面板对没选中的那条显示「⏹ 未使用（你选的是另一个）」而不是「未运行」。适配器 `display_name` / 设置标签 / boot 注册名一并对齐 |
| 观察 | `harness.log` 追加模式、token 堆叠，排障像"反复重启" | ✅ **本轮修** | 每次启动写一行 `===== harness 启动 <时间> pid=<pid> =====`（`harness_proc._read_output`），多段日志一眼分清。测试 `tests/test_harness_local_install.py::LogSeparatorTests` |
| **B2** | 系统代理毒化 `localhost` 探活 → harness 明明在跑却显示 idle | ✅ **本轮修** | 见下 §二.2 |
| B3 | npm 的 `node-safe-delete-shim` 拦截批量删除，npx 缓存半残 | ✅ 已处置 | 两条一起解决：安装侧装 dsh 时带 `CODEBUDDY_SAFE_DELETE_ENABLED=0`（本轮 B5 的永久安装里固定带上），且改成绝对路径直连后**根本不再走 npx 缓存**；SKILL.md 常见失败表有这一行 |
| B4 | 中途 kill 导致 `node-pty` 装残（`ERR_MODULE_NOT_FOUND`） | ✅ 已处置 | 技能脚本不给 npm 阶段设短超时/强杀（B5 重写时保持）；装完做**关键文件自检**（`lib/bin.js` + 任何 `node-pty` 的 `package.json`/`lib/index.js`），不齐就报错并给出"删掉重跑"的修法 |
| **B5** | `npx -y` 冷启动 2 分 10 秒 → 本地永久安装后 9 秒 | ✅ **本轮修** | 技能脚本（Windows `Prepare-Agent` / macOS `prepare_agent`）改成 `npm install @deepseek-ai/dsh@0.1.5-rc.2` 到 `<安装目录>/harness/dsh`，并把 `"<node 全路径>" "<…>/lib/bin.js" web` 写进 `harnessCommand`；npx 仅作兜底。见下 §三 |
| B6 | `PUT /api/settings` 必须包一层 `{"values": {...}}` | 📄 契约，非 bug | 技能/文档写清即可；可选：接口兼容裸对象（避免每个 agent 都踩一次） |
| **说话人分离** | 用户以为"要自己去 HF 同意条款" | ✅ 已变 | `82dbfa4c`：三件套改 **ModelScope 匿名可下**，面板/技能都能一键装；条款仍需用户自行确认（不改许可证事实） |

## 二、本轮修掉的三条（含测试）

### 1. 说话人分离的**下载落点**（你点名的那条）

`scripts/install_pyannote.py` 把落点写死成 `MODEL_ROOT = <安装根>/models/pyannote`，
而就绪判据 `modelinfo.ready_pyannote()` 查的是**可配置的** `models_dir()`（`modelsDir`）。
默认值相同时看不出来，**一旦用户在技能第 1 步填了"模型放哪"，就会下到 A 地、查 B 地** ——
装完永远显示未就绪。

修法：新增 `model_root()`，**与就绪判据同源**（`app.paths.models_root()` + `"pyannote"`），
`download()` 与 `complete()` 都改用它；`complete()` 的默认根也改成运行时解析。
注意 `scripts/` 下直接运行时 `sys.path[0]` 是 `scripts/`，所以函数里自己把 ECHO 根塞进 `sys.path`，
否则 `import app` 会失败、悄悄退回兜底路径（那就等于没修）。

测试：`tests/test_pyannote_install.py` 新增"落点跟随 `modelsDir`"与"写与查同源"两例。

### 2. B2：本机回环调用被系统代理劫走（**静默失效**）

新增 `app/netlocal.py`：

* `is_loopback(url)` —— `127.0.0.0/8`、`localhost`、`::1` 全认；
* `urlopen()` —— 按 URL 分派：回环走**无代理 opener**，外部地址**仍走系统代理**
  （企业网里访问 PyPI/ModelScope 往往正需要它，不能一刀切）；
* `ensure_no_proxy_env()` —— 把回环地址补进 `NO_PROXY`/`no_proxy`，给子进程与
  requests/httpx/huggingface_hub 兜底（已有的域名不丢）；
* `install_loopback_bypass()` —— 进程启动时装一次**全局转发**。

为什么用全局转发而不是逐个改调用点：仓库里有 **10 处** `urlopen` 直接打 `127.0.0.1`
（面板自查、模型路由探活、harness/DSH 适配器…），逐个改既慢又**必然会漏**（以后新加的又忘）。
`app/main.py` 在 import 阶段就装上（越早越好）。

> Windows 上有个必须知道的细节：`urllib` 的 `proxy_bypass` 读的是**注册表**里的 ProxyOverride，
> **光设环境变量并不保证绕过** —— 这就是为什么主要手段是"显式无代理 opener"，环境变量只是兜底。

测试：`tests/test_netlocal.py`（12 例）：回环/外部分派、无代理 opener 里没有非空代理、
`NO_PROXY` 幂等且不覆盖已有值、全局开关可装可撤、`Request` 对象也认。

### 3. B1：桌面快捷方式启动找不到 node

* **平台接缝**新增 `node_dirs()`（win32：托管式 WorkBuddy → nvm → 官方安装包；darwin：Homebrew/volta/nvm；
  linux：/usr/local/bin 等），`app/platform/__init__.py` 给中立访问器；
* `app/harness_proc.py` 新增 `find_node_dir()` / `ensure_node_on_path()`，
  在 `ensure_running()` **最开头**调用。

关键点（同事自己排查出来的）：`shutil.which("npx")` 读的是**本进程**的 `os.environ`，
早于给子进程准备的那份 env —— **只在 Popen 前构造 env 完全没用**。
在 `env = dict(os.environ)` 之前改，子进程顺带也继承到。

测试：`tests/test_harness_node_path.py`（8 例）：探测跳过没有 npx 的目录、幂等注入、
找不到就不动 PATH、`_argv()` 能解析出绝对路径。本机实测接缝探到
`C:\Users\zhkq\.workbuddy\binaries\node\versions\22.22.2-3`（正是同事报的那种托管 node）。

## 三、最该接着做的一条：B5（同事列为 P0）

| 启动方式 | 就绪耗时（同事实测） |
|---|---|
| `npx -y @deepseek-ai/dsh web` | **2 分以上**（1403 行 `npm warn cleanup`） |
| `node <本地 bin.js> web` | **9 秒 / 9 秒** |

建议按同事的方案把默认改成"**本地永久安装 + 绝对路径直连**"，npx 只当兜底：

1. 技能脚本 `Prepare-Agent`：现在是"`npx -y @deepseek-ai/dsh --version` 预热"，
   改成 `npm install @deepseek-ai/dsh@<版本> --no-audit --no-fund` 到 `<dest>\harness\dsh`；
2. 自检 `package.json` / `lib/index.js` / `prebuilds/win32-x64/conpty.node` 齐全（B4 的坑）；
3. 写 `harnessCommand = "<node 绝对路径>" "<…>\lib\bin.js" web`（ECHO 会自己追加 `--port/--no-open`）；
4. npx 预热仅作为兜底路径保留。

顺带的收益：绝对路径写法同时绕开 B1（PATH）与 B3（shim）—— 这两条本轮虽已从产品侧兜住，
但安装侧改成直连仍然更稳。

另需注意的两个"安装侧纪律"：**npx/npm 阶段禁止中途强杀**（B4 就是这么来的）；
**安装完成仍要 `POST /api/install/report`**（否则面板会一直提示"还没装完"）。

## 四、技能材料需要同步的地方

1. **重新出包**：同事那份是 `ba877dc9` / `82dbfa4c` 之前的版本（A2/A5/说话人分离三条白踩）。
2. SKILL.md 里"要不要说话人分离 → 不要（需 HF 授权）"已过时：现在 **ModelScope 匿名可下、能一键装**，
   只是"使用条款请自行确认"。
3. SKILL.md 的常见失败表补两条：B3（`SAFE_DELETE_BULK_CONFIRM_REQUIRED` →
   带 `CODEBUDDY_SAFE_DELETE_ENABLED=0`）、B6（`PUT /api/settings` 要包 `{"values": …}`）。
4. 技能第 1 步问"模型放哪"时，提醒一句：**填了自定义目录时，引擎模型与 pyannote 都会落到那里**
   （本轮已把 pyannote 的落点修成跟随 `modelsDir`）。

## 五、还差的一条：A6（每步独立日志 + 从上次成功处继续）

现状：两边技能脚本**已经幂等**（重跑会跳过已装好的：引擎依赖、harness 本地件、已就绪的模型），
每一步都有输出，失败会非 0 退出并列出是哪几项 —— 但**同一台机器上"断了以后从哪儿接着跑"仍然靠人看输出判断**。

要做的话，建议这么做（未做，先记下来，别做成半截）：

1. ~~每步写独立日志~~ **已做**（`<安装目录>/data/logs/install-<时间戳>.log`，与终端输出同源）；
2. 给步骤补一个编号（现在 `step` 只有名字），启动时读当天最新那份日志，若最后一条是"某步开始但没看到
   该步的 ok/warn"，就**只提示**"上次停在第 N 步，重跑即可跳过前面的" —— 真正的跳过仍由幂等判断负责，
   别做两套真相。

为什么不做成"记一个断点文件自动续跑"：同事那台机器上是**权限/网络**问题，
自动续跑会把"装了一半"的状态固化下来，反而更难查。日志 + 幂等重跑已经够用。
