# P3 收口施工方案（待执行，2026-09-19 拟定）

> 这份文件是**照着做就能跑完**的清单。起因：本会话命令通道故障（见 §1），
> 一批路径层改动已经写好但**没有跑过门禁**，而"守卫测试"必须先有清点才能写。
> 相关背景与决策见 `docs/2.0-PROGRESS.md` §27–§29。

## 0. 本次已改但未验证（14 个文件）

| 组 | 文件 | 性质 |
|---|---|---|
| 地基 | `app/paths.py` | `echo_root()` 支持 `ECHO_ROOT` 环境变量覆盖 |
| 地基 | `app/config.py` | `expand_path()` 的 `{ECHO}`/`{DATA}` 改由路径层解析（修 split-brain 与 mac 的 `{DATA}`） |
| 地基 | `app/db.py`、`app/manager.py` | `BASE_DIR`/`DATA_DIR` 走路径层（仍是模块级常量，请求时解析留 P3 后续） |
| 真 bug | `app/assistant.py` | `CAPTURES_DIR` 原本写进**安装目录**的 `data/` → 改走**数据根**（macOS 上会落进 .app） |
| 一致性 | `app/audio/tts.py`、`app/main.py`、`app/meeting.py`、`app/audio/stt.py`、`app/runtime.py`、`app/modelinfo.py` | `BASE_DIR` 等改走 `paths.echo_root()`（取值不变） |
| 测试 | `tests/test_paths.py` | 新增 3 组：`ECHO_ROOT`/`ECHO_DATA` 覆盖语义、`expand_path` 走路径层、**已收口清单**不许再自己推根（守卫种子，现 10 个文件） |
| 工具 | `scripts/audit-paths.py`（新） | 只读清点器：`PATH_DERIVATION` / `DRIVE_LITERAL` / `PLATFORM_TOKEN` 三类违规 |
| 文档 | `docs/2.0-PROGRESS.md` | §27–§29 记录（含"竖线是面板装饰"与音频排查挂起） |

## 1. 前置：恢复验证能力（10 秒）

本会话工作目录 `C:\Users\zhkq\Desktop\学习\ECHO-public` **被删除**，Node 起子进程时以它当
`cwd`，于是命令通道报 `spawn C:\WINDOWS\...\powershell.exe ENOENT`（exe 明明在磁盘上），
`grep`/`glob` 一并报 provider failure。**建回那个空目录即可，不必重启 DSH**；或新开会话、
工作区选 `C:\echo-dev`。

## 2. 验收顺序（照抄）

| 步 | 命令 | 期望 |
|---|---|---|
| 1 | `powershell -ExecutionPolicy Bypass -File scripts\check-windows.ps1` | 五项全 PASS（compileall app/mac/scripts、import smoke、platform contract、unit tests、ruff F821/E9） |
| 2 | `git status --short` | 只应出现 §0 的 14 个文件（其中 1 个新文件） |
| 3 | 按 §3 切分提交 | 每个 commit 前重跑步 1 |
| 4 | `python scripts\audit-paths.py` | 出违规清单（此刻应仍有若干 `PATH_DERIVATION`） |
| 5 | 按 §4 顺序收口，每收几处重跑步 1 | 门禁保持绿 |
| 6 | `python scripts\audit-paths.py --check` | 退 0（白名单只剩 `app/platform/`、`app/paths.py`） |
| 7 | 把 audit 逻辑提升为 `tests/test_path_seam.py` | 门禁仍绿（形态见 §6） |

> **2026-09-19 上午：七步全部走完**，结果见 §10（含 audit 工具自身 6 处精度问题的更正）。

## 3. 建议的提交切分

- **commit A（路径层地基）**：`app/paths.py`、`app/config.py`、`app/db.py`、`app/manager.py`
- **commit B（根用错 / 一致性收口）**：`app/assistant.py`、`app/audio/tts.py`、`app/main.py`、
  `app/meeting.py`、`app/audio/stt.py`、`app/runtime.py`、`app/modelinfo.py`
- **commit C（守卫种子 + 工具 + 文档）**：`tests/test_paths.py`、`scripts/audit-paths.py`、
  `docs/2.0-PROGRESS.md`、`docs/P3-收口施工方案.md`

> `pre-push` 钩子会挡非 noreply 邮箱的提交（本机生效，见 §25 的记录）。

## 4. 剩余收口顺序（等 audit 清单，别提前盲改）

1. **`PATH_DERIVATION`**：逐个换成 `paths.echo_root()`。**取值不变的先做**；凡是把结果
   拼进 `data/…` 的，单独判断该用 `paths.data_root()`（那才是真 bug，`assistant.py` 就是例子）。
2. **`DRIVE_LITERAL`**：字符串里的 `C:\…` → 配置项或 `{ECHO}`/`{DATA}` 占位符。注意本工具
   **不扫 `.ps1`**（脚本侧见 §5）。
3. **`PLATFORM_TOKEN`**：**triage，不要机械清零**——
   - 展示类保留（例：`app/services.py:23` 的 `platform.system()` 拼状态文案）；
   - 分支类搬进 `app/platform/<os>/`（D12）：`os.name`、`sys.platform`、`LOCALAPPDATA`、
     `SystemRoot`、`ProgramFiles` 等。
4. 每类做完把 `ALLOWED` 白名单缩到只剩 `app/platform/`。

### 4.1 手工清点已确认的点（门禁不可用期间读代码找到的，**未改**）

| 位置 | 问题 | 该怎么做 |
|---|---|---|
| `app/audio/wake.py:17-18` | `KWS_MODEL_DIR = {ECHO}/models/wakeword/kws-zh-en-3m` —— **不跟随 `modelsDir`**，而配置说明里明确承诺"唤醒词"随它走（D20） | 照 P1 把 `MEETINGS_DIR → meetings_dir()` 的做法：转成 `kws_model_dir()` 函数 + 改本文件调用点 |
| `app/audio/diarize.py:25-29` | `PYANNOTE_DIR`/`SEG_DIR`/`EMB_DIR`/`PLDA_DIR` 同理（配置说明里也点名了 pyannote） | 同上，转成函数并改调用点 |
| `app/modelinfo.py:36` | `HF_HOME` 钉在安装目录的 models（见 §28.4/§28.5） | 启动阶段统一设（D30） |
| `app/api.py`（1077 行）、`app/agents/*`、`app/hotkey.py`、`app/pathutil.py`、`app/netguard.py`、`app/pathadmin.py`、`app/ports.py`、`app/components.py`、`app/audio/recorder.py`、`app/single_instance.py` | **尚未读完**，可能有更多 `__file__` 推根 | 由 `audit-paths.py` 补齐（它的规则已覆盖 `os.path.*` 与 `Path(__file__).parent` 两种写法） |

### 4.2 清点进度表（门禁不可用期间逐文件读出来的）

| 文件 | 状态 |
|---|---|
| `app/paths.py`、`app/platform/*` | 唯一合法的推根处（白名单） |
| `config.py`、`db.py`、`manager.py`、`assistant.py`、`main.py`、`meeting.py`、`audio/stt.py`、`runtime.py`、`modelinfo.py`、`llm_router.py`、`audio/tts.py` | ✅ 已收口（其中 db/manager/main/meeting/stt/llm_router 只读了头部与改动区，**后段仍待 audit 复核**） |
| `app/pathutil.py`（20 行）、`app/dsh.py`（32 行）、`app/hotkey.py`（232 行） | ✅ **全文已读，无推根** |
| `services.py`、`boot.py`、`worklog.py`、`single_instance.py` | ✅ 头部已读，无推根（`single_instance.acquire(name, data_dir)` 的 data_dir 是**参数**，天然与安装位置无关） |
| `app/api.py`（1077 行） | ⏳ 只读了前 55 行（无推根），其余待 audit |
| `app/agents/dsh_agent.py`（647 行） | ⏳ 只读了前 32 行 |
| `app/agents/base.py`（96 行）、`app/netguard.py`（99 行）、`app/pathadmin.py`（122 行）、`app/audio/recorder.py`（253 行） | ✅ 头部已读、无推根；`pathadmin.py` 尤其干净——所有动作都收 `root` **参数** |
| `app/components.py`（251 行） | ⏳ 头部无模块级推根（清单根在函数内取），待 audit 确认 |
| `app/ports.py`（209 行） | ✅ 端口文件走 `db.DATA_DIR`，无 `__file__` 推根 |
| `app/audio/wake.py`、`app/audio/diarize.py` | ❌ **已确认违规**（模型目录不跟随 `modelsDir`，见 §4.1） |

> **清点手法与它的局限**：根推导几乎总是**模块级常量**（就在文件头部），所以头部通读能覆盖
> 绝大多数情况；但"读头部没看到"不等于"文件里没有"——**只有脚本能证明**。因此表里所有 `✅`
> 在门禁恢复后一律以 `audit-paths.py` 复核为准。

### 4.2.1 §4.2 表的更正（门禁恢复后，脚本为准）

门禁一通就跑了清点器：它报出 7 处 `PATH_DERIVATION`，其中 **4 处与上表冲突** ——
"只读头部"确实会漏（脚本同时覆盖 `os.path.*` 与 pathlib 两种写法）：

| 文件 | §4.2 原判 | 脚本实测 | 结论 |
|---|---|---|---|
| `app/failover_proxy.py` | **表里根本没有这个文件** | `:35 BASE_DIR = dirname(dirname(abspath(__file__)))` | 手工清点漏项，已收口 |
| `app/main.py` | ✅ 已收口 | `:27 _FH_DIR = dirname(dirname(abspath(__file__)))`（faulthandler 块） | 同文件第二处，已收口（改走 `paths.data_root()`） |
| `app/single_instance.py` | ✅ 全文已读、无推根 | `:150`/`:181` 的 `data_dir=None` **默认值**自己推 `<root>/data` | 参数是干净的，默认值不是；已收口 |
| `app/agents/base.py` | ✅ 头部已读、无推根 | `:96 ECHO_WORKSPACE = dirname×3(__file__)` | 已收口 |

另有 1 处脚本误报（已改规则）：`app/audio/diarize.py:56` 的
`os.path.dirname(speechbrain.__file__)` 是**第三方包自己的位置**，与安装根无关 ——
规则改为只抓**裸** `__file__`（`X.__file__` 不算）。

`PLATFORM_TOKEN` 方面：`app/components.py` 的 12 处 `"win32"` 是**清单中立标记**
（`app/platform/__init__.py` 明文规定），不是平台分支，也已从规则里排除。

其余上表标 ✅ 的文件（config / db / manager / assistant / meeting / audio-stt /
runtime / modelinfo / llm_router / audio-tts / pathutil / dsh / hotkey / services /
boot / worklog / components / ports / netguard / pathadmin / audio-recorder 等）
经脚本复核**确实干净**。

> 这张表的用途：门禁一恢复，**先跑 `audit-paths.py` 与这张表对照**——表里标"✅ 无推根"的文件
> 若被脚本报出违规，说明我手工清点漏了（脚本比人可靠），以脚本为准；表里"⏳"的以脚本补齐。

> 这几个"模型目录不跟随 `modelsDir`"的点是**真 bug**，不是纯一致性：`app/config.py` 里
> `modelsDir` 的说明写的是"模型权重（whisper / SenseVoice / 唤醒词 / pyannote…）的存放目录"，
> 而现在唤醒词与 pyannote 两处不认这个配置。它们需要动调用点，所以留给门禁可用时做。

## 5. 非 Python 侧（同一原则：自定位、零绝对路径）

- 桌面快捷方式 `.lnk`、开机 `vbs`（由 `scripts\install.ps1` 生成，含本机绝对路径，已在
  `.gitignore` 里）——生成物不入库，但**生成脚本要自定位**；
- `ECHO_PYTHON` 那条 junction 逻辑（`scripts\startup.ps1:78-84`）：保留，但注释要写明它只为
  "自身路径非 ASCII"服务，ASCII 树不得借用别的树的 venv；
- 新脚本一律自定位：参照 `C:\echo1.0\scripts\probe-tts.bat` 的 `%~dp0` 写法。

## 6. 守卫测试的最终形态（照 `tests/test_platform_contract.py` 的范式）

那个文件已经给出了本仓库认可的写法，直接沿用即可：

```python
ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

def _py_files(*dirs):        # 遍历 .py，跳过 __pycache__ / build
def _import_lines(path):     # 逐行去掉注释，只看代码

class PathDerivationStaysInPathsLayer(unittest.TestCase):
    def test_only_paths_py_derives_the_install_root(self):
        bad = []
        for path in _py_files("app"):
            rel = os.path.relpath(path, ROOT).replace("\\", "/")
            if rel in ("app/paths.py",) or rel.startswith("app/platform/"):
                continue
            for i, code in _import_lines(path):
                if "dirname" in code and "__file__" in code:
                    bad.append("%s:%d: %s" % (rel, i, code))
        self.assertEqual(bad, [], "安装根只能由 app/paths.py 推导：\n" + "\n".join(bad))
```

**为什么现在不写**：写出来就会红，而清单还没拿到（`app/api.py` 等文件我还没读全）。
把"红着的测试"留在树上比没写更糟——先让它绿，再进去。

## 7. 顺带要做的两件（都在门禁可见范围内）

1. `scripts\startup.ps1`：`Start-Process` 加 `-X utf8`。现在 `data\logs\echo-server.log` 是
   **GBK**（重定向未指定编码），工具与 grep 都读不了——排查时是个硬障碍。
2. **HF_HOME 启动落点**（设计见 §28.4/§28.5）：`app/main.py` lifespan 里
   `settings.seed_defaults()` 之后、`boot.setup()` 之前，按 `paths.models_root()` 设一次；
   **建议只在用户显式配置了 `modelsDir` 时才覆盖**，这样默认安装零行为变化。
   （`huggingface_hub` 在自己 import 时就把缓存路径算死，所以必须赶在任何 HF 代码之前。）

## 8. 不要做的事

- 不要用模块 `__getattr__` 兼容旧常量名：PEP 562 只管属性访问，模块内部**裸名字**不走它，
  会炸一片 `NameError`（1.x → 2.0 已经踩过一次）。
- 不要在拿到 audit 清单前盲改 `app/`：`Settings._load()` 会对**所有**配置值调用
  `expand_path()`，一处改错是全量配置受影响。
- 不要把**安装根**做成面板配置项，只保留 `ECHO_ROOT` 环境变量；用户可配的只有数据类目录
  （`ECHO_DATA` / `meetingsDir` / `modelsDir`）。


---

## 9. 交接单（门禁不可用期间结束时的状态）

**一句话**：路径层地基 + 11 个文件的收口已经写好，**没跑过门禁、没提交**。

**未提交的 17 个文件**（提交切分见 §3）：

```
app/paths.py  app/config.py  app/db.py  app/manager.py  app/assistant.py  app/main.py
app/meeting.py  app/runtime.py  app/modelinfo.py  app/llm_router.py
app/audio/tts.py  app/audio/stt.py
tests/test_paths.py  scripts/audit-paths.py
docs/2.0-PROGRESS.md  docs/P3-收口施工方案.md  docs/REFACTOR-PLAN.md
```

**问题清单与状态**：

| # | 问题 | 状态 |
|---|---|---|
| 1 | `assistant.CAPTURES_DIR` 写进**安装目录**（macOS 上会落进 `.app`） | ✅ 已改，待门禁 |
| 2 | `config.expand_path()` 的 `{ECHO}`/`{DATA}` 各推一遍（split-brain + mac 的 `{DATA}` 错） | ✅ 已改，待门禁 |
| 3 | `audio/wake.py` 的 KWS 模型目录、`audio/diarize.py` 的四个 pyannote 目录**不跟随 `modelsDir`** | ❌ 未改（要动调用点），见 §4.1 |
| 4 | `modelinfo.py` 的 `HF_HOME` 不跟随 `modelsDir`，且**受导入顺序影响** | ❌ 未改（方案 D30） |
| 5 | 媒体键"没捉到" | ✅ 查清：**媒体键正常**（08:46:39/08:55:55 两条 mediakey 命令），缺的是**声音** |
| 6 | ECHO 完全没声音（提示音 + 简报都听不到） | ⏳ 挂起；探针 `C:\echo1.0\scripts\probe-tts.bat` 还没跑 |
| 7 | 面板首页卡片只显示问题、不显示简报 | ⏳ UX 缺口 |
| 8 | `data\logs\echo-server.log` 是 **GBK**，工具/grep 读不了 | ⏳ 待改（§7 第 1 条） |

**需要你决定的两件事**：

1. **安装根要不要做成面板配置项？** 我按 D27 定为"只做 `ECHO_ROOT` 环境变量"（理由见
   `REFACTOR-PLAN.md` §15）。如果你更想在面板里改，说一声——建议做成"只读展示 + 环境变量写入"，
   而不是可随手保存的表单项（配错=全盘静默跑偏）。
2. **`C:\echo1.0\scripts\` 下我加的两个诊断文件**（`probe_tts.py`、`probe-tts.bat`）留不留？
   稳定树是冻结的；留着方便你双击排查，介意就删。

**回到开发的最短路径**：

```powershell
# 1) 让命令通道复活（二选一）
mkdir "C:\Users\zhkq\Desktop\学习\ECHO-public"      # 或：新开会话、工作区选 C:\echo-dev
# 2) 验证这批改动（五项全 PASS 才提交）
powershell -ExecutionPolicy Bypass -File C:\echo-dev\scripts\check-windows.ps1
# 3) 出真实清单（与 §4.2 的表对照；脚本为准）
& C:\echo-dev\venv\Scripts\python.exe C:\echo-dev\scripts\audit-paths.py
```

---

## 10. 收口结果（2026-09-19 上午执行；§9 交接单已清）

**门禁**：`check-windows.ps1` 五项全 PASS（compileall / import smoke / platform
contract / **206** unit tests / ruff F821+E9）；每个 commit 前都重跑过。

**§3 的提交切分**（§9 的 17 个文件；注意 §3 的清单**漏了** `llm_router.py` 与
`REFACTOR-PLAN.md`，本次按 17 文件补齐）：

| commit | 内容 |
|---|---|
| `6f49ac5` | A 路径层地基：paths / config / db / manager |
| `c94a534` | B 根用错与一致性收口（8 个文件，含补上的 llm_router.py） |
| `7d0187f` | C 守卫种子 + audit 工具 + 文档（含 S8 更正） |

**§4 清点与收口（脚本为准）**

- 先修**清点器自身的 4 个精度缺陷**（`506a96d`）：
  1. 盘符正则把 `http://` 当盘符 → 19 条几乎全是 URL 误报；
  2. docstring 被判成表达式字符串 → config.py:38 / api.py:789 的正文泄漏进报告；
  3. `os.name` / `sys.platform` 的属性式判定**从未生效**（tokenize 在点号两侧插空格，
     用 `"os.name" in code` 这种子串匹配永远失败）→ §19 数出的 12 处 `os.name` 全部隐身；
  4. Python 3.11 把 f-string 当**一个** STRING token → services.py:23 的
     `platform.system()` 隐身（§28.6 以为"audit 会报"，其实不会）。
  另排除两类设计内误报：清单中立标记 `"win32"`（components.py ×12）、第三方包位置
  `speechbrain.__file__`。
- `DRIVE_LITERAL` 修规则后 **0**；`PATH_DERIVATION` 7 处全收口 → **0**（`e357459`）；
  `PLATFORM_TOKEN` 14 处全收进接缝 → **0**（`1c56279`）。
- `audit-paths.py --check` **退 0**，白名单只剩 `app/platform/` + `app/paths.py`。

**§4.1 的三处真 bug**（`e357459`、`1830870`）
- `audio/wake.py` 的 KWS 目录 → `kws_model_dir()`；`audio/diarize.py` 的四个 pyannote
  目录 → 四个函数；全部跟随 `modelsDir`（`modelinfo._pyannote_command()` 的 `--local-dir` 一并跟随）。
- **D30**：lifespan 在 `seed_defaults()` 后、`boot.setup()` 前按 `paths.hf_home()` 设
  `HF_HOME`（未配 `modelsDir` 时**不动** = 默认安装零行为变化）；两处 `setdefault` 降级为兜底。

**守卫**（`aab8305`）：`tests/test_path_seam.py`（12 用例）。**复用 `scripts/audit-paths.py`
的规则实现**（报告与守卫不会漂移），附加三类断言：① 白名单必须**恰好**是 platform 与 paths.py；
② 扫描器自证（违规必报、URL/文档串/第三方 file 位置必不报）；③ 三平台 env.py 必须实现同一组原语。

**诚实边界**：三条规则 ≠ D12 全部。清点器**不扫** Windows API（`ctypes.windll`、`winsound`、
`winreg`、`msvcrt`）、`tasklist`、硬编码 `0x08000000` 这类"运行时替换"——§19 统计的 13 处
Windows API 导入、边条/热键/TTS 的平台实现仍散在业务代码里，本轮**没有**一并搬迁。
