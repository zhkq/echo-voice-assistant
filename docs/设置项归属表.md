# ECHO 设置项归属表（从 `app.config.DEFAULTS` 机械导出，106 项）

> 本表由 `scripts/gen-settings-map.py` 生成，**不要手工编辑**（会与代码漂移）。
> 改归属请改生成器里的 `SPECIAL` / `GRP_RULES`，然后重跑。

**规矩：不能莫名取消。** 每一项都必须落在三种处置之一 —— **保留** / **合并到 X** / **已过时（写明被谁取代）**；
归属没定的写「**待定**」，等拍板，绝不静默消失。

| 处置 | 含义 |
|---|---|
| 保留 | 原样存在，只挪位置 / 改展示（常用 or 高级） |
| 合并 → X | 并入另一项（老库按迁移表折算，行为不变） |
| 已过时 | 有据可查的重复项 / 不再需要的项，写明取代者 |

**统计**：保留 = 98 · 合并 = 3 · 已过时 = 5

## 现分组 `panel`（7 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `serverPort` | ECHO 面板端口 | int |  | 常规 / 服务 | 高级 | 保留（面板端口） |
| `panelHotkey` | 仪表盘热键 | str |  | 常规 / 界面 | 常用 | 保留（待确认归属） |
| `panelOpenMode` | 仪表盘打开方式 | str：sidebar/app/browser |  | 常规 / 界面 | 常用 | 保留（待确认归属） |
| `panelAutoStart` | 启动时自动显示折叠条 | bool |  | 常规 / 界面 | 常用 | 保留（待确认归属） |
| `panelStartCollapsed` | 自动显示时收起为折叠条 | bool |  | 常规 / 界面 | 常用 | 保留（待确认归属） |
| `panelAutoRefresh` | 面板自动刷新秒 | int |  | 常规 / 界面 | 常用 | 保留（待确认归属） |
| `apiAuthEnabled` | API 鉴权（手机 App） | bool |  | 常规 / 界面 | 常用 | 保留（待确认归属） |

## 现分组 `voice`（34 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `commandWorkspace` | 命令会话工作区 | str |  | 智能体 / 工作区 | 常用 | 保留（指令空间） |
| `commandWorkspaceTitle` | 指令分组名 | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `commandIdleRotateHours` | 命令会话空闲轮换小时 | float |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `commandTargetWorkspace` | 命令目标工作区（面板同步） | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `commandTargetSession` | 命令目标会话（面板同步） | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `userLocation` | 用户所在地 | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `sendEnvContext` | 发送环境上下文 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `sttLanguage` | 转写语言 | str：zh/en/ja/ko/yue/auto |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `triggerKeys` | 媒体键触发 | list：vol_up/vol_down/play_pause/next/prev |  | 语音与设备 / 命令采集 | 高级 | 保留（**仍是逗号文本框，语义待改**） |
| `wakeHotkey` | 唤醒热键 | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `fallbackHotkey` | 回退热键 | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `silenceThreshold` | 静音阈值 | float |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `silenceHangoverMs` | 静音收尾毫秒 | int |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `noSpeechAbortMs` | 无语音放弃毫秒 | int |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `maxRecordMs` | 最长录音毫秒 | int |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `inputDeviceId` | 默认输入设备 | str |  | 语音与设备 / 命令采集 | 常用 | 保留（收音设备） |
| `commandInputDeviceId` | 指令/唤醒输入设备 | str |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `meetingInputDeviceId` | 会议录音输入设备 | str |  | 语音与设备 / 会议录音 | 常用 | 保留（会议麦克风） |
| `outputDeviceIds` | 扬声器优先级 | list |  | 语音与设备 / 任务反馈 | 高级 | 保留（**仍是单选，语义待改** —— 它其实是优先级列表） |
| `commandOutputDeviceId` | 指令播报扬声器 | str |  | 语音与设备 / 任务反馈 | 高级 | 保留 |
| `meetingOutputDeviceId` | 会议播报扬声器 | str |  | 语音与设备 / 任务反馈 | 高级 | 保留（实现方建议：留在任务反馈） |
| `allowVirtualInputDevice` | 允许使用虚拟/接力输入设备 | bool | 是 | 语音与设备 / 命令采集 | 高级 | 保留（**当前无下发通道**：voice 组 hidden 且不在 /api/capability 的 settings 里；要可编辑得后端加） |
| `consumeMediaKey` | 拦截媒体键 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `beepOnStart` | 开始提示音 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `beepOnDone` | 停录提示音 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `beepOnSend` | 发送提示音 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `notifyOnSend` | 桌面通知 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `ttsEngine` | 语音合成引擎 | str：auto/edge-tts/sapi/off |  | 语音与设备 / 任务反馈 | 常用 | 保留（朗读） |
| `voiceConfirm` | 语音复述确认 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `voiceBrief` | 语音简报 | bool |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `maxBriefChars` | 简报最大字数 | int |  | 语音与设备 | 高级 | 保留（待确认归属） |
| `minimalReply` | 要求极简回复 | bool |  | 常规 / 界面 | 高级 | 保留（同上；原来归「语音与设备/任务反馈」，实现方指出应合到一起） |
| `minimalReplyChars` | 极简回复字数上限 | int |  | 常规 / 界面 | 高级 | 保留（同上） |
| `minimalReplyHint` | 极简回复要求文案 | str |  | 常规 / 界面 | 常用 | 保留（与 minimalReply/minimalReplyChars 同页，别再劈开） |

## 现分组 `paths`（2 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `meetingsDir` | 会议文件目录 | str |  | 语音与设备 / 会议录音 | 常用 | 保留（音频存放；与会议工作区同目录，**界面应显示解析后的真实路径**） |
| `modelsDir` | 模型目录 | str |  | 智能体 / 工作区 | 常用 | 保留（待确认归属） |

## 现分组 `model`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `device` | 计算设备 | str：auto/cpu/cuda |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `sttModel` | 命令转写引擎 | str：sensevoice/qwen3asr/sherpa/tiny/base/small/medium/large |  | 语音与设备 / 命令采集 | 常用 | 保留（转写引擎） |
| `wakeEngine` | 唤醒方式 | str：sherpa/kws |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `meetingSttModel` | 会议转写引擎 | str：sensevoice/qwen3asr/sherpa/small/medium/large |  | 能力后端 / 本机 | 常用 | 保留（本机跑会议时的引擎） |
| `meetingDiarize` | 区分说话人 | bool |  | 语音与设备 / 会议录音 | 常用 | 保留（实现方指出放「命令采集」不合理，改到会议录音） |
| `voiceprintEnabled` | 声纹识别常用联系人 | bool |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `voiceprintAutoEnroll` | 改名时自动入库声纹 | bool |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `voiceprintThreshold` | 声纹匹配阈值 | float |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `voiceprintMargin` | 声纹歧义间隔 | float |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |

## 现分组 `wake`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `wakeEnabled` | 启用语音唤醒 | bool |  | 语音与设备 / 命令采集 | 常用 | 保留 |
| `wakePaused` | 唤醒暂停（勿扰） | bool |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeKeywords` | 唤醒词 | list |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeAliases` | 唤醒词别名（误听容错） | list |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeThreshold` | 唤醒灵敏度阈值 | float |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeCooldownSec` | 触发冷却秒 | float |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeConfirmX` | 确认帧数 | int |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeConfirmN` | 确认窗口 | int |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `wakeSilenceFloor` | 静音门控 | int |  | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |

## 现分组 `meeting`（5 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `meetingSegmentMinutes` | 分段分钟 | int |  | 语音与设备 / 会议录音 | 常用 | 保留（分段时长） |
| `meetingAutoSummarize` | 自动生成纪要 | bool |  | 语音与设备 / 会议录音 | 高级 | 保留（待确认归属） |
| `meetingKeepRawAudio` | 保留原始音频 | bool |  | 语音与设备 / 会议录音 | 高级 | 保留（待确认归属） |
| `meetingWorkspace` | 会议会话工作区 | str |  | 智能体 / 工作区 | 常用 | 保留（会议工作区） |
| `meetingWorkspaceTitle` | 会议分组名 | str |  | 语音与设备 / 会议录音 | 高级 | 保留（待确认归属） |

## 现分组 `worklog`（5 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `worklogEnabled` | 启用纪要归档 | bool |  | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `worklogEnsureSessionAccess` | 归档前校正会话权限 | bool |  | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `worklogVaultRoot` | 笔记库根目录 | str |  | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `worklogMode` | 归档方式 | str：skill/off |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `worklogPrompt` | 归档提示词模板 | str |  | 智能体 / 智能体 | 高级 | 保留（待确认归属） |

## 现分组 `dsh`（3 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `dshStartCommand` | DSH 启动子命令 | str |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `dshNodePath` | Node 可执行文件路径 | str |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `dshPackageDir` | DSH 包目录 | str |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |

## 现分组 `agent`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `dshBaseUrl` | DSH 服务地址 | str | 是 | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `agentBackend` | 执行智能体 | str：dsh/codebuddy/harness | 是 | 智能体 / 智能体 | 常用 | 保留（选哪个智能体） |
| `agentCodebuddyEnabled` | 启用 CodeBuddy Code | bool | 是 | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `agentCustomPath` | CLI 路径 | str | 是 | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `agentHarnessEnabled` | 启用标准版 harness（DeepSeek Harness） | bool | 是 | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `harnessHome` | harness 数据目录（DSH_HOME） | str | 是 | 智能体 / 智能体 | 常用 | 保留（家目录） |
| `harnessPort` | harness 端口 | int | 是 | 智能体 / 智能体 | 高级 | 保留（待确认归属） |
| `harnessCommand` | harness 启动命令 | str | 是 | 智能体 / 智能体 | 高级 | 保留（启动命令覆盖） |
| `harnessToken` | harness 访问 token | str | 是 | 智能体 / 智能体 | 高级 | 保留（待确认归属） |

## 现分组 `provider`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `providerAsr` | 转写 provider | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（转写引擎下拉里的「在线服务」） |
| `providerLlm` | 语言模型 provider | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `providerTts` | 朗读 provider | str | 是 | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `providerLlmBaseUrl` | 在线 LLM 地址 | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `providerLlmApiKey` | 在线 LLM 密钥 | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `providerLlmModel` | 在线 LLM 模型名 | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（待确认归属） |
| `providerAsrBaseUrl` | 在线转写地址 | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（同上） |
| `providerAsrApiKey` | 在线转写密钥 | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（密钥，接口永不回显） |
| `providerAsrModel` | 在线转写模型名 | str | 是 | 语音与设备 / 命令采集 | 高级 | 保留（同上） |

## 现分组 `capability`（7 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `capabilityEchoServerUrl` | ECHO 能力后端地址 | str | 是 | 能力后端 / 会议转写服务 | 高级 | 保留（待确认归属） |
| `capabilityEchoServerToken` | ECHO 能力后端令牌（手工填） | str | 是 | 能力后端 / 后端面板 | 高级 | 保留（**当前无下发通道**：ROUTING_KEYS 刻意排除，怕与配对令牌混淆） |
| `capabilityEchoServerStaticToken` | ECHO 能力后端静态令牌 | str | 是 | 能力后端 / 后端面板 | 高级 | 保留（同上） |
| `capabilityPrivacy` | 允许音频去哪 | str：none/lan/wan | 是 | 能力后端 / 会议转写服务 | 高级 | 保留（出网许可，约束三个后端；界面层实现方建议与选择一致性一起显示） |
| `capabilityMeetingAsrBackend` | 会议转写用哪个后端 | str：echo-server/local/asr-provider | 是 | 能力后端 / 会议转写服务 | 常用 | 合并 → 会议转写服务（ECHO 后端/本机/网络服务商） |
| `capabilityDiarizeBackend` | 说话人分离用哪个后端 | str：auto/echo-server/local/off | 是 | 能力后端 / 会议转写服务 | 常用 | 合并 → 会议产出（全部/说话人/只要文字） |
| `capabilityEmbedBackend` | 声纹提取用哪个后端 | str：auto/echo-server/local/off | 是 | 能力后端 / 会议转写服务 | 常用 | 合并 → 会议产出（全部/说话人/只要文字） |

## 现分组 `router`（7 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `routerAutoRegister` | 启动时注册到 DSH | bool |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |
| `routerDisplayName` | 模型组显示名 | str |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |
| `routerProbeInterval` | 健康探测间隔秒 | int |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |
| `routerFirstByteTimeout` | 成员首字节超时秒 | float |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |
| `routerConnectTimeout` | 成员连接超时秒 | float |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |
| `routerBreakerThreshold` | 熔断连续失败次数 | int |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |
| `routerBreakerCooldown` | 熔断冷却秒 | int |  | 智能体 / 通道设置 | 高级 | 保留（待确认归属） |

## 待定项清单（需要拍板）

（无）
