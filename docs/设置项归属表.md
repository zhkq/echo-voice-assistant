# ECHO 设置项归属表（从 `app.config.DEFAULTS` 机械导出，108 项）

> 本表由 `scripts/gen-settings-map.py` 生成，**不要手工编辑**（会与代码漂移）。
> 改归属请改生成器里的 `SPECIAL` / `GRP_RULES` / `DEPRECATED_NOTES`，然后重跑。

**规矩：不能莫名取消。** 每一项都必须落在三种处置之一 —— **保留** / **合并到 X** / **已过时（写明被谁取代）**；
归属没定的写「**待定**」，等拍板，绝不静默消失。

| 处置 | 含义 |
|---|---|
| 保留 | 原样存在，只挪位置 / 改展示（常用 or 高级） |
| 合并 → X | 并入另一项（老库按迁移表折算，行为不变） |
| 已过时 | 有据可查的重复项 / 不再需要的项，写明取代者 |

**统计**：保留 = 100 · 合并 = 1 · 已过时 = 7

## 现分组 `panel`（7 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `serverPort` | ECHO 面板端口 | int |  | ECHO 通用 / 运行状态 | 高级 | 保留（面板端口） |
| `panelHotkey` | 仪表盘热键 | str |  | ECHO 通用 / 快捷键与打开方式 | 常用 | 保留（待确认归属） |
| `panelOpenMode` | 仪表盘打开方式 | str：sidebar/app/browser |  | ECHO 通用 / 快捷键与打开方式 | 常用 | 保留（待确认归属） |
| `panelAutoStart` | 启动时自动显示折叠条 | bool |  | ECHO 通用 / 快捷键与打开方式 | 常用 | 保留（待确认归属） |
| `panelStartCollapsed` | 自动显示时收起为折叠条 | bool |  | ECHO 通用 / 快捷键与打开方式 | 常用 | 保留（待确认归属） |
| `panelAutoRefresh` | 面板自动刷新秒 | int |  | ECHO 通用 / 快捷键与打开方式 | 常用 | 保留（待确认归属） |
| `apiAuthEnabled` | API 鉴权（手机 App） | bool |  | ECHO 通用 / 快捷键与打开方式 | 常用 | 保留（待确认归属） |

## 现分组 `voice`（34 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `commandWorkspace` | 命令会话工作区 | str |  | 业务配置 / 工作区与归档 | 常用 | 保留（指令空间） |
| `commandWorkspaceTitle` | 指令分组名 | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `commandIdleRotateHours` | 命令会话空闲轮换小时 | float |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `commandTargetWorkspace` | 命令目标工作区（面板同步） | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `commandTargetSession` | 命令目标会话（面板同步） | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `userLocation` | 用户所在地 | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `sendEnvContext` | 发送环境上下文 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `sttLanguage` | 转写语言 | str：zh/en/ja/ko/yue/auto |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `triggerKeys` | 媒体键触发 | list：vol_up/vol_down/play_pause/next/prev |  | 业务配置 / 语音指令 | 高级 | 保留（**仍是逗号文本框，语义待改**） |
| `wakeHotkey` | 唤醒热键 | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `fallbackHotkey` | 回退热键 | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `silenceThreshold` | 静音阈值 | float |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `silenceHangoverMs` | 静音收尾毫秒 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `noSpeechAbortMs` | 无语音放弃毫秒 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `maxRecordMs` | 最长录音毫秒 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `inputDeviceId` | 默认输入设备 | str |  | 业务配置 / 语音指令 | 常用 | 保留（收音设备） |
| `commandInputDeviceId` | 指令/唤醒输入设备 | str |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `meetingInputDeviceId` | 会议录音输入设备 | str |  | 业务配置 / 会议 | 常用 | 保留（会议麦克风） |
| `outputDeviceIds` | 扬声器优先级 | list |  | 能力与智能体 / 设备选择 | 常用 | 保留（播放设备池与优先级） |
| `commandOutputDeviceId` | 指令播报扬声器 | str |  | 能力与智能体 / 设备选择 | 常用 | 保留（同上） |
| `meetingOutputDeviceId` | 会议播报扬声器 | str |  | 能力与智能体 / 设备选择 | 常用 | 保留（同上） |
| `allowVirtualInputDevice` | 允许使用虚拟/接力输入设备 | bool | 是 | 业务配置 / 语音指令 | 高级 | 保留（**当前无下发通道**：voice 组 hidden 且不在 /api/capability 的 settings 里；要可编辑得后端加） |
| `consumeMediaKey` | 拦截媒体键 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `beepOnStart` | 开始提示音 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `beepOnDone` | 停录提示音 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `beepOnSend` | 发送提示音 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `notifyOnSend` | 桌面通知 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `ttsEngine` | 语音合成引擎 | str：auto/edge-tts/sapi/off |  | 业务配置 / 朗读与反馈 | 常用 | 保留（朗读） |
| `voiceConfirm` | 语音复述确认 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `voiceBrief` | 语音简报 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `maxBriefChars` | 简报最大字数 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `minimalReply` | 要求极简回复 | bool |  | 业务配置 / 朗读与反馈 | 高级 | 保留（同上） |
| `minimalReplyChars` | 极简回复字数上限 | int |  | 业务配置 / 朗读与反馈 | 高级 | 保留（同上） |
| `minimalReplyHint` | 极简回复要求文案 | str |  | 业务配置 / 朗读与反馈 | 高级 | 保留（与 minimalReply/minimalReplyChars 同卡，别再劈开） |

## 现分组 `paths`（2 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `meetingsDir` | 会议文件目录 | str |  | 业务配置 / 会议 | 常用 | 保留（音频存放；与会议工作区同目录，**界面应显示解析后的真实路径**） |
| `modelsDir` | 模型目录 | str |  | 能力与智能体 / 本地能力 | 常用 | 保留（模型权重放哪） |

## 现分组 `model`（10 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `device` | 计算设备 | str：auto/cpu/cuda |  | 能力与智能体 / 设备选择 | 常用 | 保留（推理设备：auto/cpu/cuda） |
| `sttModel` | 命令转写引擎 | str：sherpa/sensevoice/qwen3asr |  | 业务配置 / 语音指令 | 常用 | 保留（转写引擎） |
| `modelCleanupDays` | 清理阈值（天） | int |  | 能力与智能体 / 清理 | 常用 | **新增**（「近期」是多少天：默认 90；0 = 不按时间过滤） |
| `wakeEngine` | 唤醒方式 | str：sherpa/kws |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `meetingSttModel` | 会议转写引擎 | str：sherpa/sensevoice/qwen3asr |  | 能力与智能体 / 本地能力 | 常用 | 保留（本机跑会议时的引擎；选择入口在「业务配置 → 会议 → 转写走哪条路」） |
| `meetingDiarize` | 区分说话人 | bool |  | — | — | **已过时**：**会议转写一律包含说话人分离**（转写 + 分离 + 声纹三件标配，本地或走 ECHO 后端都一样）——「要不要分离」不再是用户设置。**分离由谁做**见 `capabilityDiarizeBackend`（「业务配置 → 会议 · 转写走哪条路」）；老库里的值只作兼容读取，改了没有任何效果 |
| `voiceprintEnabled` | 声纹识别常用联系人 | bool |  | — | — | **已过时**：**声纹识别（认人）是标配**，没有开关 —— 只要库里已经有这个人，会议就显示姓名（库空时静默无结果、零副作用）。「要不要把声音**存进**库」仍由用户决定，见 `voiceprintAutoEnroll`与会议详情里的「声纹入库」按钮 |
| `voiceprintAutoEnroll` | 改名即入库 | bool |  | 业务配置 / 语音指令 | 高级 | 保留（**改名即入库**：唯一与声纹有关的开关，默认关） |
| `voiceprintThreshold` | 声纹匹配阈值 | float |  | 业务配置 / 语音指令 | 高级 | 保留（认错人时唯一能调的东西） |
| `voiceprintMargin` | 声纹歧义间隔 | float |  | 业务配置 / 语音指令 | 高级 | 保留（同上） |

## 现分组 `wake`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `wakeEnabled` | 启用语音唤醒 | bool |  | 业务配置 / 语音指令 | 常用 | 保留 |
| `wakePaused` | 唤醒暂停（勿扰） | bool |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeKeywords` | 唤醒词 | list |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeAliases` | 唤醒词别名（误听容错） | list |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeThreshold` | 唤醒灵敏度阈值 | float |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeCooldownSec` | 触发冷却秒 | float |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeConfirmX` | 确认帧数 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeConfirmN` | 确认窗口 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `wakeSilenceFloor` | 静音门控 | int |  | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |

## 现分组 `meeting`（6 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `meetingSegmentMinutes` | 分段分钟 | int |  | 业务配置 / 会议 | 常用 | 保留（分段时长） |
| `meetingAutoSummarize` | 自动生成纪要 | bool |  | 业务配置 / 会议 | 高级 | 保留（待确认归属） |
| `meetingKeepRawAudio` | 保留原始音频 | bool |  | 业务配置 / 会议 | 高级 | 保留（待确认归属） |
| `meetingAutoCompressAudio` | 转写完成后自动压缩 | bool |  | 业务配置 / 会议 | 高级 | 保留（待确认归属） |
| `meetingWorkspace` | 会议会话工作区 | str |  | 业务配置 / 工作区与归档 | 常用 | 保留（会议工作区） |
| `meetingWorkspaceTitle` | 会议分组名 | str |  | 业务配置 / 会议 | 高级 | 保留（待确认归属） |

## 现分组 `worklog`（5 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `worklogEnabled` | 启用纪要归档 | bool |  | 业务配置 / 工作区与归档 | 高级 | 保留（待确认归属） |
| `worklogEnsureSessionAccess` | 归档前校正会话权限 | bool |  | 业务配置 / 工作区与归档 | 高级 | 保留（待确认归属） |
| `worklogVaultRoot` | 笔记库根目录 | str |  | 业务配置 / 工作区与归档 | 高级 | 保留（待确认归属） |
| `worklogMode` | 归档方式 | str：skill/off |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `worklogPrompt` | 归档提示词模板 | str |  | 业务配置 / 工作区与归档 | 高级 | 保留（待确认归属） |

## 现分组 `dsh`（3 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `dshStartCommand` | DSH 启动子命令 | str |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `dshNodePath` | Node 可执行文件路径 | str |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `dshPackageDir` | DSH 包目录 | str |  | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |

## 现分组 `agent`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `dshBaseUrl` | DSH 服务地址 | str | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（待确认归属） |
| `agentBackend` | 执行智能体 | str：dsh/codebuddy/harness | 是 | 能力与智能体 / 智能体选择 | 常用 | 保留（选哪个智能体） |
| `agentCodebuddyEnabled` | 启用 CodeBuddy Code | bool | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（待确认归属） |
| `agentCustomPath` | CLI 路径 | str | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（待确认归属） |
| `agentHarnessEnabled` | 启用标准版 harness（DeepSeek Harness） | bool | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（待确认归属） |
| `harnessHome` | harness 数据目录（DSH_HOME） | str | 是 | 能力与智能体 / 智能体选择 | 常用 | 保留（家目录） |
| `harnessPort` | harness 端口 | int | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（待确认归属） |
| `harnessCommand` | harness 启动命令 | str | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（启动命令覆盖） |
| `harnessToken` | harness 访问 token | str | 是 | 能力与智能体 / 智能体选择 | 高级 | 保留（待确认归属） |

## 现分组 `provider`（9 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `providerAsr` | 转写 provider | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（转写引擎下拉里的「在线服务」） |
| `providerLlm` | 语言模型 provider | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `providerTts` | 朗读 provider | str | 是 | — | — | **已过时**：`deprecated=True`，任何接口都不下发（点名的替代项见说明） |
| `providerLlmBaseUrl` | 在线 LLM 地址 | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `providerLlmApiKey` | 在线 LLM 密钥 | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `providerLlmModel` | 在线 LLM 模型名 | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（待确认归属） |
| `providerAsrBaseUrl` | 在线转写地址 | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（同上） |
| `providerAsrApiKey` | 在线转写密钥 | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（密钥，接口永不回显） |
| `providerAsrModel` | 在线转写模型名 | str | 是 | 业务配置 / 语音指令 | 高级 | 保留（同上） |

## 现分组 `capability`（7 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `capabilityEchoServerUrl` | ECHO 能力后端地址 | str | 是 | 能力与智能体 / 已配对后端 | 高级 | 保留（直连后端的地址；配对优先） |
| `capabilityEchoServerToken` | ECHO 能力后端令牌（手工填） | str | 是 | 能力与智能体 / 已配对后端 | 高级 | 保留（**当前无下发通道**：ROUTING_KEYS 刻意排除，怕与配对令牌混淆） |
| `capabilityEchoServerStaticToken` | ECHO 能力后端静态令牌 | str | 是 | 能力与智能体 / 已配对后端 | 高级 | 保留（同上） |
| `capabilityPrivacy` | 允许音频去哪 | str：none/lan/wan | 是 | 业务配置 / 会议 | 高级 | 保留（出网许可，约束三个会议后端） |
| `capabilityMeetingAsrBackend` | 会议转写用哪个后端 | str：echo-server/local/asr-provider | 是 | 业务配置 / 会议 · 转写走哪条路 | 常用 | 合并 → 会议「转写走哪条路」单选（ECHO 后端/本机/网络服务商） |
| `capabilityDiarizeBackend` | 说话人分离用哪个后端 | str：auto/echo-server/local | 是 | 业务配置 / 会议 · 转写走哪条路 | 常用 | 保留（谁做说话人分离；**会议一定会分离**，这里只选由谁做） |
| `capabilityEmbedBackend` | 声纹嵌入用哪个后端 | str：auto/echo-server/local | 是 | 业务配置 / 会议 · 转写走哪条路 | 常用 | 保留（谁做声纹嵌入；识别是标配，入库由用户决定） |

## 现分组 `router`（7 项）

| 键 | 标签 | 类型/选项 | 隐藏 | 归属（页签 / 卡片） | 层级 | 处置 |
|---|---|---|---|---|---|---|
| `routerAutoRegister` | 启动时注册到 DSH | bool |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |
| `routerDisplayName` | 模型组显示名 | str |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |
| `routerProbeInterval` | 健康探测间隔秒 | int |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |
| `routerFirstByteTimeout` | 成员首字节超时秒 | float |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |
| `routerConnectTimeout` | 成员连接超时秒 | float |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |
| `routerBreakerThreshold` | 熔断连续失败次数 | int |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |
| `routerBreakerCooldown` | 熔断冷却秒 | int |  | 能力与智能体 / 模型路由 · 路由参数 | 高级 | 保留（待确认归属） |

## 待定项清单（需要拍板）

（无）
